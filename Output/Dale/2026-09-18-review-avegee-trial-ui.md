# Review: AVEGEE trial/battle UI batch (`2ac2dd4..21a14df`, 8 commits)

**ผู้ตรวจ:** Dale · **ขอบเขต:** `2ac2dd4^..21a14df` (feat(battle) → 21a14df fix .who2 wrap)
**อ้างอิง:** สเปก Vera (`Output/Vera/2026-09-18-avegee-trial-battle-ui.md`), ใบงาน Toby, รายงาน Toby ทั้งสองฉบับ

## ผลตรวจ: **PASS**

ทำงานจริงบนเบราว์เซอร์ (Playwright/Chromium, ไม่ใช่แค่อ่านโค้ด) ที่ 1440×900 / 390×844 / 360×640
เข้าเกมจริง (เริ่มเกมใหม่ → ข้ามบทนำ → กด "เริ่มการสอบสวน") ไม่ใช้ debug hook ค้างจาก session ก่อน
(session นี้ไม่มี `window.__dbg` — ใช้ `window.G` ที่โค้ดปัจจุบัน expose ไว้จริง `src/ui.js:2385`)

---

## 1. `node --check` + manifest path

- `node --check` ผ่านทั้ง 12 ไฟล์ใน `src/*.js` — ไม่มี syntax error
- เขียนสคริปต์ไล่ทุก path ใน `img/manifest.json` (`critical` + `rest` + `zones.{asia,west,cyberhell}`
  รวม 215 รายการ) เทียบกับไฟล์จริงบนดิสก์ — **resolve ครบ 215/215 ไม่มีไฟล์หาย**
- โซน 1–3 ไม่พัง: เล่นจริงจบ 1 เกม (สอบสวน → ต่อสู้ → ปิดคดี) ที่ทั้ง 3 ขนาดจอ ไม่มี error

## 2. กลไกเกมไม่เปลี่ยน — diff เทียบ parent ของ batch นี้ (`2ac2dd4^` = `eb44cd5`)

หมายเหตุ: โจทย์ให้ diff เทียบ `285d59c` แต่ commit นั้นเก่ากว่าขอบเขตรีวิว 8 commit นี้ไปอีก 9 commit
(รวมงานชุด A–G ที่ถูกรีวิวแยกไปแล้วใน `408d694 worklog: Dale — Dale review batch A-G`) ผมจึง diff เทียบ
`2ac2dd4^` (จุดก่อนเริ่ม batch นี้จริง ๆ) แทน เพื่อไม่ปนกับงานที่ผ่าน gate ไปแล้วรอบก่อน — ถ้าต้องการให้ diff
เทียบ 285d59c เต็มช่วงด้วยแจ้งได้ ทำเพิ่มได้ไม่ยาก

- `git diff 2ac2dd4^..21a14df --stat -- src/` → **มีแค่ `src/ui.js` เปลี่ยน (38 บรรทัด)** `game.js`/`data.js`/
  `scene.js`/`room.js`/`walk.js` **ไม่ถูกแตะเลย**
- diff เต็มของ `src/ui.js`: ทุกจุดที่เปลี่ยนเป็นการย้าย/ห่อ HTML template ใหม่ (`.hud-menu`/`.seg`/
  `.go-cluster`, เพิ่ม `.corner-tick` 4 span) — เงื่อนไข logic เดิมทั้งหมดอยู่ตำแหน่งเดิมคำต่อคำ:
  `trialCmd === k`, `heaven ? '' : cmd('inten', …)`, `g.needBattle(s)`, `ready`, `g.queue.length > 1`,
  `g.has('tarang')`, `g.jailFree()` — ไม่มีตัวไหนถูกแก้ค่า/สลับลำดับ
- `id`/`data-*` ทุกตัวคงเดิม: `data-cmd="ask|st|cr|inten"`, `id="t-go"`, `id="t-skip"`, `id="t-jail"`
- `index.html` diff (238 บรรทัด) เช็คแล้ว**ไม่มี `<script>` เปลี่ยนเลยสักบรรทัด** — เป็น CSS ล้วน 100%

## 3. ทดสอบเบราว์เซอร์จริงที่ 1440 / 390×844 / 360×640

ทดสอบด้วย Playwright ควบคุมเกมจริงผ่าน `window.G` (expose จริงจากโค้ด ไม่ใช่ debug hook ที่ผมเพิ่มเอง):

| เช็ค | 1440 | 390×844 | 360×640 |
|---|---|---|---|
| กดปุ่มเมนู 4 ปุ่ม (`ask/st/cr/inten`) | ✅ ทั้ง 4 เจอ+กดได้ | ✅ | ✅ |
| `#t-skip` (บังคับ `queue.length=2`) — กดแล้วปิด dialog, ไม่มี error | ✅ | — | ✅ |
| `#t-jail` (บังคับมีสถานี `tarang`) — กดแล้ว `g.held.length` 0→1, ปิด dialog | — | — | ✅ |
| `#t-go` ไม่มีขัดขืน — ปิดคดีจริง `queue.length` 1→0, `dialog` ปิด, ไม่มี error | ✅ | — | — |
| `#t-go` มีขัดขืน (บังคับ `soul.resist=true`) → เข้าฉากต่อสู้ `.arena` เปิดจริง | ✅ | ✅ | ✅ |
| กด "ฟาด" ในฉากต่อสู้ 1 ครั้ง — เลือดคู่ต่อสู้ลด 90→75/90, บทพูดเปลี่ยน | ✅ | — | ✅ |
| จี้ประเด็น (`data-line`) — log อัปเดต ไม่มี error | ✅ | ✅ | ✅ |
| `.hud-rec.scrollHeight === clientHeight` (ไม่สกอลล์ภายใน) | ✅ (0 diff) | ✅ | ✅ |
| `#t-go` อยู่ในวิวพอร์ตเสมอ (bounding rect ภายในจอ) | ✅ | ✅ | ✅ |
| `dialog.hudwrap` กว้าง 1320px คงที่ตลอดสลับแท็บ (กันบั๊กหดแคบเดิม) | ✅ 6/6 ครั้ง | — | — |
| console error ใหม่ | ไม่มี (เหลือแค่ 404 เดิม: `hero-yama-side.png`, `audio/bgm-*` — pre-existing ไม่เกี่ยวงานนี้) | เหมือนกัน | เหมือนกัน |

(สกรีนช็อตของผม: `/tmp/dale-1440-trial.png`, `/tmp/dale-m390-01-open.png`, `/tmp/dale-m360-01-open.png`,
`/tmp/dale-d1440-battle-01-arena.png`, `/tmp/dale-m360-battle-02-afterAtk.png`, `/tmp/dale-360-cr-tab.png`
— session-local `/tmp`, capture ใหม่ได้ด้วยสคริปต์ scratchpad ถ้าต้องการ)

## 4. เมนูชิดคอลัมน์ขวา + อ่านง่ายที่ 360×640

ยืนยันด้วยสายตาจากสกรีนช็อตจริง (ไม่ใช่แค่เชื่อรายงาน Toby): เมนู 6 ปุ่ม (กริด 3×2) อยู่เหนือ
`.hud-right` (แฟ้มคดี) พอดี กว้างเท่ากับคอลัมน์ขวาทั้งแท่ง — ตรงกับที่คุณเป้ขอ ("ชิดคอลัมน์ขวา ... กว้างเท่ากัน")
ที่ 360×640 ตัวอักษรบนปุ่ม + sub-label ("จี้ได้อีก 2 ครั้ง", "ยังไม่เลือก", "3 ปานกลาง") อ่านออกชัดเจน
ไม่มีตัวไหนถูกตัดคำกลางคำ

## 5. พื้นผิวกระดาษ `.hud-rec` + ภาษาไทยที่ 360×640

Crop + zoom 3x เฉพาะกล่อง `.hud-rec` ที่ 360px (`/tmp/dale-360-hudrec-zoom3x.png`) — เห็นลายกระดาษ/
หนังเก่าเป็นจุดด่างสีน้ำตาลไม่สม่ำเสมอชัดเจน ไม่ใช่พื้นเรียบแบนอีกต่อไป ตัวอักษรไทย (ขาว/เขียว/ทอง บนพื้นม่วง
เข้ม) คอนทราสต์พออ่านสบาย ไม่มีจุดไหนที่ลายกระดาษกลืนกับตัวอักษร — สูตรผสมสีที่ Toby แก้ (สลับลำดับเลเยอร์ +
ลด opacity สต็อปล่าง `.92→.68`) ใช้ได้จริงตามที่รายงาน

## 6. `BG-Sala-west` → `BG-Tea-west` rename

- `grep -rn "BG-Sala-west"` ทั้ง repo (`.js`/`.html`/`.json`) → **ไม่มีการอ้างชื่อเดิมเหลือเลย**
- ไล่ chain การ resolve เอง (ไม่เชื่อแค่คำอธิบาย): `openStation('tea')` → `stBg('tea')` =
  `artUrl('BG-Tea', 'webp')` (`src/ui.js:1874`) → โซน west ต่อท้าย `-west` ให้เอง (`zoneStem`, `art.js`)
  → `BG-Tea-west.webp` ตรงกับไฟล์จริงใน `img/West/` และ manifest `zones.west` เป๊ะ
- `curl` ไฟล์ตรง ๆ ได้ 200 · เทียบกับสกรีนช็อตห้องศาลาน้ำชาของ Toby (`/tmp/tea-station.png`) เป็นฉากโรงเตี๊ยม
  จริง ไม่ใช่ fallback

## 7. โปรไฟล์ยมทูตโซน 4 (CyberHell) + โซน 4 ยังเล่นไม่ได้

- ทั้ง 5 ไฟล์ (`crew-{boon,dam,kan,plerng,taan}-cyberhell-profile.png`) มีอยู่จริงและ resolve ผ่าน manifest
  check ข้อ 1 แล้ว (215/215 รวมกลุ่มนี้)
- `grep "ZONES"` ใน `src/data.js` — โซน 4 (`k:'cyberhell'`) นิยามอยู่นอก `ZONES[]` จริงตามคอมเมนต์ในโค้ด
  บรรทัด 1306: "ตั้งใจไม่ใส่ใน ZONES[] ตามใบงาน" — ยืนยันเล่นไม่ได้จริงตามที่ตั้งใจ

## 8. สองข้อสังเกตของ Toby

**(a) `.hud` ทั้งกล่องยังสกอลล์ได้ ~56–74px ที่ 360×640 กับคดียาวผิดปกติ** — ผมวัดเองได้ diff 56–74px
สอดคล้องกับที่ Toby รายงาน (~58px) เกณฑ์รับงานที่ Vera ล็อกไว้ระบุเฉพาะ **`.hud-rec` ห้ามสกอลล์ภายในตัวเอง**
กับ **`#t-go` ต้องอยู่ในจอเสมอ** — ทั้งสองข้อผ่าน 100% ในการทดสอบของผม สกอลล์ทั้งกล่องที่เหลือกระทบแค่
`.hud-bottom` (การ์ดรูปยมน้อย/วิญญาณ) ซึ่งสเปกข้อ 2 ของหัวข้อ "หมายเหตุการอ่านตัวเลข" เขียนไว้ชัดว่าเป็น
"เกราะสำรอง" ที่ตั้งใจไว้อยู่แล้ว ไม่ใช่บั๊กใหม่จากงานนี้ → **ยอมรับได้ ไม่ใช่ FIX LIST** แต่ควรอยู่ในลิสต์งาน
ถัดไปถ้าคุณเป้อยากให้เป็นศูนย์แม้กรณีคดียาวผิดปกติ (ต้องลด `.hud-stage`/`.hud-top` เพิ่มอีก กระทบขนาดตัวละคร)

**(b) ไฟล์กำพร้า `BG-Sala-cyberhell.webp` / `st-sala-cyberhell.png`** — ตรวจแล้วไฟล์ทั้งสอง **ไม่ใช่ขยะที่ไม่ได้
ใช้** จริง ๆ: ทั้งคู่ถูกอ้างอยู่ใน `manifest.json` (`zones.cyberhell` + `boxes['st-sala-cyberhell']`) เตรียมไว้
สำหรับสถานี "sala" (หอทะเบียนกรรม) ของโซน 4 ที่ยังไม่เปิดเล่น — เทียบกับโซน 2 (Asia) ที่มี `BG-Sala-asia.webp`
พร้อม raw (`img/raw/Asia/BG-Sala-asia.jpeg`) ปกติ แต่โซน 4 **ไม่มี raw ใน `img/raw/CyberHell/`** เลย
(`ls` ยืนยันว่างเปล่าเฉพาะคู่นี้) — สรุป: **อย่าลบ** (ลบแล้วสถานี sala ของโซน 4 จะไม่มีฉากห้องตอนเปิดเล่นจริง)
แต่ควรถามคุณเป้/Mind ว่ามี raw เก็บไว้ที่อื่นไหม (เช่น export เดิมก่อนย้ายโฟลเดอร์) ถ้าไม่มีจริง ให้บันทึกไว้ใน
`CONCEPT.md` หรือ backlog โซน 4 ว่า "BG-Sala-cyberhell เป็น exception ไม่มี raw ที่ตรวจสอบ/แก้ไขซ้ำได้"
ก่อนเปิดโซน 4 ให้เล่นจริง — ไม่ใช่เรื่องเร่งด่วนตอนนี้เพราะโซน 4 ยังปิดอยู่

## 9. ข้อสังเกตเล็กที่ไม่ถึงขั้น FIX LIST

- ขนาด `.port img` วัดจริงได้ **92/70/58px** (desktop/390/360) เทียบสเปกที่ขอ 96/72/60px — ต่างกัน 2-4px
  (~4%) น่าจะมาจาก border-box กิน border 2px เข้าไปในขนาดจริง ผลลัพธ์ที่เห็นด้วยตายังคือ "ใหญ่ขึ้นชัดเจน"
  ตามคำสั่งคุณเป้ (จากเดิม 52/44px) ไม่กระทบเกณฑ์รับงาน ไม่บังคับแก้
- ฟอนต์/white-space ของ `.port .who2 b`/`small` ที่ 360 วัดได้ตรงสเปกเป๊ะ (13px/10.5px, `nowrap`+`ellipsis`)
  ยืนยันบั๊กตกบรรทัดที่ Toby แก้ใน `21a14df` **ใช้ได้จริง** — ทดสอบซ้ำที่มุมที่เคยพัง (แท็บ "ใครคุม" ทำการ์ดกว้าง
  ขึ้น) ไม่มีการซ้อนทับ `.hud-log` อีก

---

## สรุป

ครบทุกเกณฑ์ที่โจทย์ให้ตรวจ (1–8) ไม่มีจุดที่ต้องแก้ก่อนรับงาน กลไกเกมไม่เปลี่ยนแม้แต่บรรทัดเดียว
(ยืนยันด้วย diff + เล่นจริงเปลี่ยนสถานะจริงผ่าน `window.G`) ภาพ/แฟ้มทุกไฟล์ resolve ครบ ไม่มี error ใหม่

**PASS**
