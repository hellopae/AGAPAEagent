# รีวิวโค้ด — AVEGEE ชุดแก้ที่ 4 (Toby, โหมด claude-only)

ผู้ตรวจ: Dale · repo `/Users/agapae/Documents/Work PAE/Claude/AVEGEE` · diff `24c45d5..9673d01` (6 commit, push แล้ว)
วิธีตรวจ: อ่านโค้ด diff ทั้งหมด + รันเกมจริงด้วย Playwright (chromium) ผ่าน `python3 -m http.server 8934`
ทดสอบ end-to-end จริงในเบราว์เซอร์ (ไม่ใช่แค่อ่านโค้ด) สำหรับข้อ A/B/D/E/F ทุกข้อ + สอบทาน deploy จริงบน Pages

## ผลตรวจ: **PASS**

## รายละเอียดตามเกณฑ์รับงาน

**1. นั่งพักที่ศาลาน้ำชา (ข้อ A)** — ผ่าน
ทดสอบจริง: สร้างศาลา, เดินเข้าไปยืนจุด `act`, ปุ่ม "นั่งพัก" เปิดใช้งานเมื่อ `inReach()` เท่านั้น (ปุ่ม disabled
ถูกต้องก่อนยืนถึงจุด) กด sit แล้วบารมีขยับจาก 10 → 24.4 ใน ~3.3 วิ (≈3.6/วิ ใกล้เคียงค่าที่ตั้ง `hpRegenSit=3`)
เบี้ยกรรมไม่ลดเลยระหว่างนั่ง (500 คงที่ที่ 380 หลังหักค่าสร้างศาลา) ปุ่ม "ลุกขึ้น" ใช้งานได้ทันทีไม่มีเงื่อนไข
รองรับไฟล์ `hero-yama-sit.png`/`hero-yama-sit-sip.png` จริง (โหลด 200 ทั้งคู่บน live Pages) โค้ด fallback
ไปท่ายืน+💤 ถ้าไม่มีไฟล์ก็อ่านแล้วสมเหตุสมผล ไม่มี console error ใหม่

**2. ปุ่มความแรง (ข้อ B)** — ผ่านทุกข้อย่อย
ทดสอบจริงที่ 6 ขนาดจอ (360/390/520/900/1100/1440) — เปิดวงคำสั่ง "ความแรง" แล้วยืนยันด้วย
`elementFromPoint` ว่าทั้ง 5 วงกลมอยู่ในวิวพอร์ตและคลิกได้จริงครบทุกจอ (กริด `repeat(5,minmax(0,1fr))`
บังคับแถวเดียวทำงานตรงตามที่อธิบาย) กดระดับ 5 แล้ว label ขึ้น "มหันต์" ตรงกัน (grep ทั้งไฟล์ไม่เหลือ "สาสม"
เลย) แผงพื้นหลัง `.command-options` เป็น `rgba(0,0,0,0)` โปร่งใสจริง (screenshot ยืนยัน — ไม่มีกล่องน้ำตาล
เหลือแต่ปุ่มกลมลอย) ไอคอน 5 วงใช้ `img/icon-force-1..5.png` จริง โหลด 200 ทั้งหมด ป้ายชื่อใต้ไอคอนอ่านออก
ชัดเจนทุกระดับหลัง fade-in เสร็จ (screenshot zoom ยืนยัน)
*หมายเหตุพบระหว่างทดสอบ (ไม่ใช่บั๊กของ batch นี้)*: การเปิดวงคำสั่งมี `onpointerenter` (hover เปิด) ซ้อนกับ
`onclick` (toggle) อยู่แล้วในโค้ดเดิม (`command-wheel.js`, ไม่ได้แก้ในชุดนี้) — เมาส์จริงจะ hover เปิดก่อนแล้ว
click จะไปปิดซ้ำได้ในบางจังหวะ ไม่กระทบการเล่นจริงเพราะผู้ใช้จริงไม่ได้ hover ผ่านจุดกึ่งกลางเป๊ะแบบสคริปต์
ทดสอบ — แจ้งไว้เผื่อ Vera/Chris อยากทบทวน ไม่ใช่ scope ของใบงานนี้

**3. วิญญาณต่อต้าน (ข้อ C)** — ไม่พบต้นเหตุเพิ่มเติม ยืนยันรายงาน Toby
ไล่โค้ดเส้นทาง `#t-go onclick` → `needBattle` → `startBattle` → `openBattle` → `openDlg('rpg')` เอง:
- `openDlg()` เรียก `dlgGen++` ก่อน `dlg.close()`/`showModal()` เสมอ — การ์ด `gen !== dlgGen` ใน `onDlgClose`
  จึงกันการยิง callback ของกล่องเก่า (bgm/refresh ของหน้าไต่สวน) ทับกล่องต่อสู้ใหม่ได้จริง ตรวจแล้วไม่รั่ว
- `drawCoach()` มีการ์ด `if (g.battle) return` และ `if (dlg.open) return` ก่อนเปิดโมดัลบทเรียนอยู่แล้ว
  (ของเดิม ไม่ใช่ของ batch นี้) กันชนกับการเปิดฉากต่อสู้ได้จริง
- delegated close handler บน `dlg` (click → `[data-close]`) ใช้ `e.target` ที่ผูกตาม dispatch-time path
  ของ DOM เดิม แม้ `go.onclick` จะแทนที่ `dlg.innerHTML` ไปแล้วในสแต็กเดียวกัน ปุ่ม `#t-go` เดิม (target)
  ไม่มี attribute `data-close` เอง จึงไม่ทริกเกอร์ปิดกล่องใหม่ที่เพิ่งเปิด — ตรวจแล้วไม่ใช่ต้นเหตุ
ไม่พบเส้นทางอื่นที่ทำให้ "เด้งออก" จากการอ่านโค้ดตรง ๆ สอดคล้องกับที่ Toby soak-test แล้วไม่ reproduce
**ข้อสังเกตที่อาจเป็นไปได้ (ทฤษฎี ยังไม่ยืนยัน)**: ปุ่ม `#t-go` ไม่มี debounce/guard กันกดซ้ำเร็ว ๆ
(เช่น double-tap/ghost-click บนมือถือบางรุ่น) ถ้าทัชสองครั้งติดกันเข้าตำแหน่งเดิมพอดีหลัง DOM เปลี่ยนเป็น
ฉากต่อสู้แล้ว อาจมีโอกาสไปโดนอะไรบางอย่างในฉากใหม่ที่ตำแหน่งเดิม แต่ตรวจโค้ดแล้วไม่พบปุ่ม/องค์ประกอบที่
"ปิดฉาก" อยู่ตรงตำแหน่งเดียวกับ `#t-go` เดิมโดยตรง — เป็นแค่ทิศทางที่ยังตรวจไม่ครบ ถ้าคุณเป้เจอซ้ำ ขอ
รายละเอียดตามที่ Toby เขียนไว้ (อุปกรณ์จริง/ขนาดจอ/สำนวนที่กำลังตัดสิน) ตามที่ระบุในรายงาน
บั๊กที่เจอระหว่างทาง (closure `coachStep` เป็น null ตอนกดปุ่มบทเรียนเก่าค้าง) ตรวจโค้ดแล้วเป็นการแก้ป้องกัน
ที่ปลอดภัย ไม่กระทบพฤติกรรมปกติ (นับเป็น "ไม่มีการแก้กลไกจริงของข้อ C" ตามที่ Toby รายงานตรงไปตรงมา)

**4. Cooldown ยมทูต 100 วินาที** — ผ่าน
`BATTLE.crewCd: 100` ยืนยันทั้งใน source และบน live Pages (`curl` ดึง `src/data.js` จริง) คู่มือในเกมดึงค่า
`${BATTLE.crewCd}` แทน hardcode แล้ว grep ทั้ง `src/*.js` ไม่พบเลข 150 หลงเหลือที่เกี่ยวกับ cooldown นี้
(150 อื่น ๆ ในไฟล์เป็นค่าคนละเรื่อง — bounty, cost สถานี, roam radius)

**5. Cutscene พลัง** — ผ่าน
ทดสอบจริง: ใช้พลัง "ตวาดข่มขู่" (roar) ในห้องสอบสวน (เดิมไม่เคยมีคัตซีนเลย) ยืนยันภาพ
`hero-yama-atk-cutscene.jpeg` ขึ้นจริง ค้างอยู่ ~1.34 วินาที (อยู่ในช่วง 1.2–1.5 ที่ขอ) และกดที่ภาพเพื่อข้าม
ได้ทันที (ปิดใน 0.1 วิหลังคลิก ก่อนครบเวลาปกติ) ตรงตามที่รายงาน

**6. ปุ่มชนะที่ชายแดน** — ผ่าน
grep ยืนยัน "รับรางวัลชายแดน" หายไปหมดจากซอร์ส เหลือแต่ "เก็บไอเท็มที่ตกอยู่" จุดเดียว ตรงตำแหน่ง
`kind === 'frontier'` เท่านั้น ไม่กระทบ label อื่น (`zoneBoss`/`mob`/default ไม่เปลี่ยน)

**7. ไม่มี console error ใหม่ / push แล้ว** — ผ่าน
รันทุกสถานการณ์ข้างต้นจริงในเบราว์เซอร์ (chromium via Playwright) — error เดียวที่พบคือ 404 ของไฟล์เสียง
`audio/bgm-*.ogg` (fallback เดิมของเกม ก่อน batch นี้ ยืนยันจากไฟล์ที่มีจริงในโฟลเดอร์ `audio/` เป็น `.mp3`
เท่านั้น ไม่ใช่ผลจาก diff นี้) ไม่มี error จากไฟล์ที่แก้ในชุดนี้เลย push แล้วที่ `origin/main` (`24c45d5..9673d01`)
ยืนยันบน GitHub Pages จริง (`https://hellopae.github.io/AVEGEE/`) — `src/data.js` โชว์ `crewCd: 100`,
`src/ui.js` โชว์ `INTENSITY` มี "มหันต์" และ "เก็บไอเท็มที่ตกอยู่", ไฟล์ภาพใหม่ทุกไฟล์ตอบ 200,
`src/command-wheel.css` บน live ตรงกับ local เป๊ะ (diff ว่าง)

## ขนาดไฟล์ภาพใหม่ (เหมาะกับ GitHub Pages)
`hero-yama-sit.png` 120KB · `hero-yama-sit-sip.png` 112KB · `icon-force-1..5.png` 63–81KB ต่อไฟล์
(รวม ~470KB ทั้งชุด) — ไม่มีไฟล์ไหนใหญ่ผิดปกติ ปกติสำหรับภาพตัวละคร/ไอคอนเกมนี้

## ข้อห้ามในใบงาน
ตรวจแล้วไม่แตะ `src/cases.js`/`src/cases-asia.js` เลย (grep ยืนยัน 0 การเปลี่ยนแปลง) `CONCEPT.md` มีการ
แก้ค้างอยู่ในเวิร์กกิ่งทรี (uncommitted, ของ Mind ไม่ใช่ของ Toby — Toby ไม่ได้ commit ไฟล์นี้เลย) `files/`
`output/` ยังเป็น untracked ตามเดิม ไม่ถูก commit

## ข้อเล็กที่แก้เอง
ไม่มี — ไม่พบจุดที่ต้องแก้เพิ่ม โค้ดสะอาด คอมเมนต์อธิบายเหตุผลครบทุกจุดที่แก้ ตรงสไตล์ไฟล์เดิม

## สรุป
**PASS** ทั้ง 6 ข้อในเกณฑ์รับงาน ข้อ C ไม่มีการแก้กลไกจริงตามที่ Toby รายงานตรงไปตรงมา — ตรวจโค้ดอิสระแล้ว
เห็นด้วยว่า guard ที่มีอยู่ (dlgGen versioning, `g.battle`/`dlg.open` check ใน drawCoach) ทำงานถูกต้องแล้ว
ไม่พบต้นเหตุเพิ่มเติมจากการอ่านโค้ด ให้ยกเป็น backlog เฝ้าดู — ถ้าคุณเป้เจอซ้ำ เก็บอุปกรณ์/ขนาดจอ/สำนวนที่
กำลังตัดสินตอนนั้นให้ Toby ไล่ซ้ำได้ตรงจุด

ปัญหาเลย์เอาต์ที่ Toby แจ้งเพิ่ม (แถบทรัพยากร `#res` บังแผนที่หลักที่จอ 390px กว้าง) อยู่นอกขอบเขตใบงานนี้
จริง เป็นเรื่องออกแบบไม่ใช่บั๊กของ batch — ส่งต่อให้ Claudy พิจารณาเข้าคิว Vera/Chris ต่อ ไม่ใช่งาน Dale

## วิธีตรวจซ้ำ
```
cd "/Users/agapae/Documents/Work PAE/Claude/AVEGEE"
python3 -m http.server 8934
# เปิด http://localhost:8934/ — ทำตามสคริปต์ทดสอบในรายงาน Toby ได้ตรง ๆ
```
Live: `https://hellopae.github.io/AVEGEE/` (deploy แล้ว ตรวจแล้วว่าตรงกับ local เป๊ะ)

## Rollback
ถ้าต้องย้อน: `git revert 24d7e28 9673d01 0aee86f 2a16719 e66c1d4 52aaa5e d340b8d` (เรียงจากใหม่ไปเก่า
ระวัง `24d7e28` เป็นของ Mind ไม่เกี่ยวกับ batch นี้ ถ้าจะย้อนเฉพาะ batch4 ให้เริ่มจาก `9673d01` ถึง `d340b8d`)
หรือ `git reset --hard 24c45d5` แล้ว force-push เฉพาะกรณีฉุกเฉินจริง ๆ (คุณเป้ต้องอนุมัติก่อน)
