# รีวิวโค้ด — AVEGEE ชุด 13 (cutscene ยมทูต / ค่าพลัง-คูลดาวน์ / เตรียมศึก / ระบบหิว) — แก้รอบ 1

**ผู้ตรวจ:** Dale · **เจ้าของงาน:** Toby · **Repo:** `/Users/agapae/Documents/Work PAE/Claude/AVEGEE`
**Commit range ที่ตรวจ:** `d6601d5..dead967` (7 commits) · **ผล: PASS**

## วิธีตรวจ

1. อ่านใบงาน + โจทย์ต้นฉบับคุณเป้ + รายงาน Toby (รวม "แก้รอบ 1") ทั้งหมด
2. `node --test tests/*.test.mjs` → **34/34 ผ่าน**
3. อ่านโค้ดจริงทีละจุดเทียบกับตาราง B และเกณฑ์รับงาน 6 ข้อ (`src/data.js`, `src/game.js`, `src/ui.js`)
4. รัน `python3 scripts/prep-art.py` จริงเพื่อยืนยันว่า `img/ui/` ไม่ถูกลบ (แล้ว revert ไฟล์ทดสอบที่สคริปต์สร้าง/แก้ระหว่างการตรวจ ด้วย `git checkout`/`git clean` เฉพาะไฟล์ที่เกิดจากการรันของผม — ไม่แตะของ Toby)
5. เปิดเกมจริงด้วย Playwright (`chromium`) ที่ 1440×900 และ 390×844: เริ่มเกมใหม่ → ข้ามบทนำ → เปิดโต๊ะนิรา → ดูการ์ดทีม/แถบหิวจริงบนจอ ถ่าย screenshot ยืนยัน ไม่มี console error ใหม่ (เจอแค่ 404 เดิม 3 รายการเท่าที่ Toby รายงานไว้)

## ผลตรวจตามเกณฑ์รับงาน 6 ข้อ

1. **คัตซีนยมทูตของตัวเอง 6 ตัว + 4 ตัวพุ่ง/กานต์-บุญไม่พุ่ง — PASS**
   - `crewCutsceneSrc(k)` (`src/ui.js:1436`) คืนไฟล์ `img/crew-<k>-cutscene.jpeg` ทั้ง 6 ไฟล์ (ตรวจแล้วมีจริงและ commit แล้วทั้ง 6: taan/plerng/dam/kan/boon/guard)
   - `playActionCutscene(k)` เรียก `crewCutsceneSrc` เมื่อ `k` เป็น `crew:<k>` หรือ `'guard'` (`src/ui.js:1450-1453`) — ไม่ใช่คัตซีนยมน้อยอีกต่อไป
   - `B.helper.lunge = !(c.k === 'boon' || c.k === 'kan')` (`src/game.js` ใน `battleAct`) — กานต์/บุญ ไม่พุ่ง ตรงตามสเปก, ที่เหลือพุ่งเข้าใส่ (ใช้ `.fig.helper.lunge` เดิม)
   - แก้บั๊กจริงที่พบระหว่างทาง (`fx.crew`) กันยมน้อยสลับท่าโจมตีของตัวเองตอนยมทูตลงมือ — ตรวจโค้ดแล้วสมเหตุสมผล ไม่กระทบท่าโจมตีของยมน้อยเอง (`usingAtk` ยังเช็ค `act.lunge==='you'` เหมือนเดิม)
   - โซน 2-4 fallback ไปโซน 1 ตามที่ยอมรับได้ (ยังไม่มีไฟล์) — ระบุสเปกภาพที่ต้อง gen ไว้ครบในรายงาน (ชื่อไฟล์/ขนาด/โฟลเดอร์) พร้อมใช้ทันทีที่มีไฟล์โดยไม่ต้องแก้โค้ดเพิ่ม

2. **ตัวเลขดาเมจ/ฮีล/คูลดาวน์ตรงตาราง B + สะกดจิต/น้ำแข็ง — PASS**
   - `CREW_POWER` (`src/data.js:394-402`): taan 35, plerng 40, dam 35, kan confuse:1, boon heal:50, guard 60 — ตรงเป๊ะ
   - `BATTLE.crewCd = 60` (60 วิ), `GUARD.battleCd = 120` (120 วิ) — ตรงเป๊ะ
   - `ITEMS.ice = { dmg:30, stun:1 }` → ในสนามจริงกลายเป็น `B.stun` (ข้ามตา) — ตรงสเปก "หยุด 1 เทิร์น"
   - สะกดจิตจริง: `confuseFoe = pw.confuse` → `B.confuse` แยกจาก `B.stun` เดิม → ตาถัดไปของศัตรู หักดาเมจใส่ตัวเอง (`B.dmg.confuseSelf`) ไม่ใช่แค่ข้ามตา — ตรงสเปก "มึน โจมตีตัวเอง" ต่างจากน้ำแข็งชัดเจนในโค้ด
   - เล่นจริงยืนยันด้วย Playwright: เปิดโต๊ะนิรา เห็นการ์ด "ทัณฑ์ ท่าสู้: ฟาดกระบอง 35 · คูลดาวน์ 60 วินาที" และ "ยักษ์ทวารบาล ท่าสู้: ฟาดแรง 60 · คูลดาวน์ 120 วินาที" ตรงกับตาราง B บนจอจริง (ไม่ใช่แค่ค่าคงที่ในไฟล์)
   - แก้เพิ่มรอบ 1: สะกดจิตฆ่าศัตรูตายพอดี → ประกาศชนะทันที (`declareWin()` เรียกซ้ำได้ 2 จังหวะ) — ตรวจโค้ดแล้วถูกต้อง มี unit test คลุม
   - unit test `tests/combat-power.test.mjs` 7 เคส ผ่านหมด

3. **การ์ดทีม/แท็บข้อมูลตรงกับค่าจริง — PASS**
   - ยืนยันด้วยตาจริงผ่าน Playwright ที่โต๊ะนิรา ทั้ง 1440 และ 390: ตัวเลขบนการ์ดตรงกับ `CREW_POWER`/`BATTLE.crewCd`/`GUARD.battleCd` ทุกตัว ไม่มีเลขลอยแยกจากกัน (ดึงจากแหล่งเดียวผ่าน `crewAbility()`)

4. **เตรียมศึก 3 ปุ่ม (พ่อค้านรก/นิรา/กินกล่องยา) + ปุ่มเข้าสู้ ใช้ได้ทุกโซน — PASS**
   - แก้รอบ 1 (`9ffb763`) เปลี่ยนจากระบบเดิม (แฟ้มหลักฐาน/คุ้มกัน/ลูกไฟ) เป็น 3 ปุ่มตามใบงานเป๊ะ: `data-prep-merchant` → `openMerchant()`, `data-prep-nira` → `openNiraOffice()`, `data-prep-med` → `useBossMedicine()` ใหม่ (ใช้หีบยาจากกระเป๋าโดยตรง กินซ้ำได้)
   - กลับหน้าเตรียมศึกอัตโนมัติ: ใช้ตัวเฝ้า `battleUI`/`setInterval` เดิมที่มีอยู่แล้วในไฟล์ (เช็คทุก 400ms ว่าฉากต่อสู้ยังไม่จบแต่ `dlg` ปิดอยู่ → เปิดกลับให้เอง) — ไม่ได้เขียน callback ใหม่ซ้อน ตรวจโค้ดแล้วไม่มีความเสี่ยง double-onClose ที่เคยเป็นปัญหาในชุดก่อน (คอมเมนต์ 17 ก.ย. เตือนไว้ ไม่ถูกแตะ)
   - `startZoneBoss()`/`startBossFight()`/`useBossMedicine()` อ่าน `this.zoneDef()` แบบไดนามิก ไม่ผูกโซนใดโซนหนึ่ง → ใช้ได้ทุกโซนจริง
   - แก้บั๊กแฝงที่เจอระหว่างทาง: `buyMerchant()` เดิมซื้อลูกไฟ/น้ำแข็งแล้วค้างในกระเป๋าใช้ไม่ได้ทันที — แก้ให้เข้า `fireAmmo`/`powerOf('ice').ammo` ตรง ๆ เหมือนเก็บจากแผนที่ ตรวจโค้ดแล้ว branch อื่น (mirror/hypno/health) ไม่ถูกกระทบ (else เดิมยังทำงานปกติ)
   - unit test ใหม่ `tests/boss-prep.test.mjs` 6 เคส ครอบคลุมครบ: โบนัสแฟ้มหลักฐานอัตโนมัติ, `prepareBoss()`/`b.prepUsed` ถูกถอดออกจริง, กินยาซ้ำได้จนของหมด, กินไม่ได้เมื่อไม่มีของ/บารมีเต็ม, กินไม่ได้หลังกด "เข้าสู้", ซื้อลูกไฟพร้อมใช้ทันที — ผ่านหมด

5. **ป้อนข้าวปั้น 3 ทาง / หิวลดจริง / เสบียงหักจริง / ไม่มีของ→ปุ่มปิด — PASS**
   - `feedCrew(k)` (`src/game.js:2397`) หัก `this.food -= BAL.feedFoodCost` จริงจากกองเสบียงกลางเดียวกับระบบอัตโนมัติเดิม, เพิ่ม `c.hunger` ไม่เกิน 100
   - 3 จุดป้อนจริงในโค้ด: โต๊ะนิรา (`openNiraOffice`, ui.js:1751), หน้าต่างสถานีที่มียมทูตคุม (ui.js:2658), แผงข้อมูลตอนกดยมทูตบนแผนที่ (`sideBody`, ui.js:454) — ทั้ง 3 จุดเรียก `hungerWidget()`/`bindHungerWidgets()` ร่วมกัน กันเลขไม่ตรงกัน
   - ยืนยันด้วยตาจริงผ่าน Playwright ที่โต๊ะนิรา: เห็นแถบ "🍙 หิว 100/100" พร้อมปุ่ม "ให้ข้าวปั้น" ใต้การ์ดทัณฑ์จริงบนจอทั้ง 1440/390
   - `hungerWidget()` ปิดปุ่มเมื่อ `g.food < BAL.feedFoodCost` พร้อม title บอกซื้อที่ไหน — ตรงสเปก
   - หิวมีผลจริงต่อความเร็วงาน: `hungerMul` คูณเข้ากับสูตร `slot.progress +=` โดยตรง (`src/game.js:1001-1003`) ไม่ใช่แค่ตัวเลขสวย
   - unit test `tests/hunger.test.mjs` 8 เคส ผ่านหมด รวมเคสเซฟเก่า

6. **เซฟเก่าโหลดได้ / unit test ผ่าน / ไม่มี console error ใหม่ / 390px ไม่ล้น / push ครบ — PASS**
   - `restore()`: `{ ...mkCrew(def), ...sv }` — เซฟเก่าไม่มี `hunger` เลยจะได้ค่าเริ่มต้น 100 จาก `mkCrew` อัตโนมัติ ตรวจโค้ดแล้วถูกต้อง มี unit test เฉพาะยืนยัน
   - `node --test tests/*.test.mjs` → **34/34 ผ่าน** (รันจริงเอง ไม่ใช่แค่เชื่อรายงาน)
   - Playwright จริง 1440/390: บูตเกม → เริ่มเกมใหม่ → ข้ามบทนำ → เปิดโต๊ะนิรา ไม่มี console error ใหม่ (เจอ 404 เดิม 3 รายการเท่าที่ Toby ระบุไว้ล่วงหน้า — เป็นไฟล์เสียง/ภาพที่ขาดอยู่ก่อนแล้ว ไม่เกี่ยวกับชุดนี้)
   - 390×844: การ์ดทีม/แถบหิวสแต็กแนวตั้งเรียบร้อย ไม่มีข้อความล้นขอบ ปุ่มกดได้ครบ (screenshot ยืนยัน)
   - `git log --oneline d6601d5..dead967` = 7 commit ครบ push แล้ว, `git status` สะอาด (เหลือเฉพาะ diff เดิมของคุณเป้เองที่มีมาก่อนชุดนี้: `CONCEPT.md`, `files/`, `img/scene-cyberhell.jpeg`, `output/` — ไม่ใช่ของ Toby)
   - `scripts/prep-art.py` แก้แล้ว: เพิ่ม `NON_ZONE_DIRS = {'ui'}` กัน 4 จุดที่เดิน loop ผ่านโฟลเดอร์ย่อยของ `img/` — รันจริงยืนยันแล้วว่า `img/ui/` (10 ไฟล์ Button*.png) ไม่ถูกลบอีกต่อไป (หมายเหตุ: การรันจริงของผมสร้างไฟล์แปลง/แก้ manifest อื่นเป็นผลข้างเคียงของการทดสอบ — revert กลับให้สะอาดแล้วด้วย `git checkout`/`git clean` เฉพาะไฟล์ที่เกิดจากการรันของผมเอง ไม่กระทบงานของ Toby)

## จุดที่ตรวจเป็นพิเศษตามที่ Claudy สั่ง — สรุปผล

| จุด | ผล |
|---|---|
| ตัวเลขพลัง/คูลดาวน์ตอนเล่นจริง (ไม่ใช่แค่ค่าคงที่) | ยืนยันบนจอจริงที่โต๊ะนิรา — ตรง |
| cutscene แต่ละตัวเป็นภาพของตัวนั้นจริง, 4 พุ่ง/กานต์-บุญไม่พุ่ง | ตรวจโค้ด `crewCutsceneSrc`+`lunge` ถูกต้อง (ไฟล์ภาพยืนยันมีจริงทั้ง 6) |
| เตรียมศึก 3 ปุ่มตามใบงาน ไม่ค้าง ไม่มี dialog ซ้อนผิด | ตรวจโค้ด `battleUI` watcher + `useBossMedicine`/`buyMerchant` ถูกต้อง ไม่มี double-onClose |
| ระบบหิว 3 ทาง เสบียงหักจริง ไม่มีของปุ่มปิด | ยืนยันบนจอจริง (โต๊ะนิรา) + โค้ด 2 จุดที่เหลือ |
| `prep-art.py` ไม่ลบ `img/ui/` | รันจริงยืนยันแล้ว 10 ไฟล์ครบ |

## หมายเหตุที่ไม่บล็อกงาน (รับทราบ ไม่ต้องแก้)

- Toby ทำเคสขอบ "สะกดจิตฆ่าศัตรูตายพอดี" ในแก้รอบ 1 แล้ว — ปิดประเด็นเดิมที่เคยติดไว้ในรายงานฉบับแรก
- คัตซีนยมทูตโซน 2-4 ยัง fallback โซน 1 ตามที่ยอมรับได้ — สเปกภาพที่ต้อง gen ระบุครบในรายงาน Toby แล้ว (ชื่อไฟล์/ขนาด 1375×768/โฟลเดอร์) ส่งต่อคุณเป้ gen ได้เลยไม่ต้องรอชื่อยมทูตโซนอื่น

## สรุป

**PASS** — ไม่มีข้อที่ต้องแก้เพิ่ม เกณฑ์รับงานทั้ง 6 ข้อผ่านครบ ตรวจทั้งจากโค้ด/unit test/เล่นจริงผ่าน Playwright จริงทั้ง 2 ขนาดจอ ไม่มีสิ่งที่ Dale ต้องแก้เองในรอบนี้ (ไม่มี fix เล็กที่พบ)

**Commit ที่ตรวจ:** `b9b1b8a`, `d22a65e`, `408355a`, `8c231e6`, `9ffb763`, `ba8260e`, `dead967`
