# Codex and Astra profile framing fix

Adjusted only the large profile-card framing for Codex and Astra. Both portraits now render at 100% width with individual vertical offsets, keeping the full hairline inside the rounded top edge while preserving the existing overlay and card layout.

Verified in the local dashboard at desktop size. Both heads have visible space above the hair, surrounding cards remain unchanged, and the browser console has no errors.
