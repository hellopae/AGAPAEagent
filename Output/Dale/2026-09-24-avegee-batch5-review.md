# รีวิวโค้ด — AVEGEE ชุดแก้ที่ 5 (Toby, 24 ก.ย. 2569)

**ผล: PASS**

ใบงาน: `Output/Claudy/briefs/2026-09-24-avegee-fix-batch5.md`
รายงาน Toby: `Output/Toby/2026-09-24-avegee-fix-batch5.md`
Repo: `/Users/agapae/Documents/Work PAE/Claude/AVEGEE`
Commits ตรวจ: `e7a965c` (C) `6e3718a` (A) `af0060b` (B) `8dd94c8` (D) — อยู่บน `main`, push แล้ว

## วิธีตรวจ

อ่าน diff ทั้ง 4 commit ครบ (`git show`) แล้วขับเกมจริงผ่าน Chrome (Playwright, `channel:'chrome'`)
ที่ 1440×900 (เมาส์) และ 390×844 (ทัช, `has_touch:true`) — ไม่ได้เชื่อ log ที่ Toby แนบมาอย่างเดียว
ขับผ่าน UI จริง (คลิก/แตะปุ่มจริงในหน้าเกม, เข้าเมนู #t-new, เลือกวงคำสั่งจริงด้วยพิกัดที่คำนวณจากสูตร
`sector()`/`point()` เดียวกับใน `command-wheel.js` เพื่อให้เมาส์ hover จริงก่อนคลิกและ reproduce race
เดิมได้ตรง ๆ) ไม่ได้ inject state ข้ามระบบยกเว้นจุดที่จำเป็น (สร้างสถานีศาลาน้ำชาให้เสร็จทันทีด้วย
`g.build('tea')` + เคลียร์ `st.build/buildWait` — เพื่อข้ามเวลาสร้างจริงเท่านั้น ไม่ได้แตะ logic ที่ตรวจ)

สคริปต์ทดสอบเก็บไว้ที่ scratchpad ของ session นี้ ไม่ได้ commit เข้า repo (ตามข้อห้ามไม่ให้แตะไฟล์นอกสโคป)

## ผลตรวจทีละข้อเกณฑ์รับงาน

**1. สลับเลือก ที่ไหน/ใครคุม/ความแรง ลำดับไหนก็ได้ ค่าไม่หาย — PASS**
เลือกสลับลำดับ ใครคุม → ความแรง → ที่ไหน (ไม่ใช่ลำดับปกติ) ด้วยเมาส์จริง (hover ก่อนคลิกเพื่อ
reproduce race เดิม) และด้วยแตะจริงที่ 390×844 — ป้าย `.command-selected` ทั้ง 3 ป้ายอยู่ครบและ
`visible` (opacity ≠ 0) ตลอด ไม่หายเมื่อไปเปิดวงถัดไป ตรงกับที่ระบุใน diff: แยกพฤติกรรม
`matchMedia('(hover:hover) and (pointer:fine)')` แก้ race ของ `pointerenter`+`click`, และเปลี่ยน CSS
จาก `.has-options` (ซ่อนทั้งหมด) เป็น `:has()` เจาะจงซ่อนเฉพาะป้ายของวงที่เปิดอยู่จริง — ตรวจโค้ดแล้ว
สมเหตุสมผล ไม่มีจุดพลาด

**2. เลือกครบแล้วออกหมายได้จริง / ขาดมีข้อความบอก — PASS**
ทดสอบทั้งสองสถานะในดะบวนการเดียวกัน (ไม่รีสตาร์ต): เลือกแค่ "ที่ไหน" ก่อน → ขึ้นข้อความ
`⚠️ ออกหมายไม่ได้ — ยังไม่ได้เลือก ใครคุม · ความแรง` และ `#t-go` มี `disabled` จริง ตรงตามเงื่อนไข
ที่ขาด (ไม่ลอยไปข้อที่เลือกแล้ว) จากนั้นเลือกอีกสองข้อให้ครบ → ข้อความหาย, `#t-go` ไม่ disabled,
กดออกหมายได้จริง (ไปต่อจนดำเนินเรื่อง ไม่มี error) ทั้ง 1440 และ 390×844

**3. บารมีในหน้าต่างศาลากับแถบหลักตรงกันทุกจังหวะ + persist หลัง reload — PASS**
ตั้ง `g.hp=60` ก่อนเข้าศาลา แล้วนั่งพักจริง เก็บตัวอย่างค่า `#st-hp-chip` / `#res-hp-chip` / `window.G.hp`
ทุก ~0.4 วินาที 5 รอบ: ค่าตัวเลขทั้งสองป้ายตรงกันทุกตัวอย่าง (61/61, 62/62, 64/64, 65/65, 66/66) —
ไม่มีจังหวะไหนที่แถบหลักค้างค่าเก่าเลย ปิดหน้าต่างแล้วแถบหลักโชว์ 66 ทันที (ตรงกับ `g.hp`)
รีโหลดหน้าแล้ว `window.G.hp` ยังเป็น 66

*หมายเหตุจากการตรวจ (ไม่ใช่บั๊กของรอบนี้):* ระบบเซฟทั้งเกมมี auto-save throttle 4 วินาที
(`src/ui.js` บรรทัด 78: `if (now > saveAt) { saveAt = now + 4000; g.save(); }`) ถ้า reload ทันทีที่ปิด
หน้าต่าง (ไม่ถึง 4 วินาที) localStorage อาจยังเป็นค่าก่อนหน้า แล้ว "ตามทัน" เองใน tick ถัดไป —
ตรวจสอบด้วย log ตรงว่าค่าจริงไล่ตามทันเสมอ (`61→64.4→76.5→88.5→100` ตามรอบ 4 วิ) นี่เป็นพฤติกรรม
เดิมของระบบเซฟทั้งเกม (ใช้กับ coin/order/karma เหมือนกันหมด) ไม่ใช่สิ่งที่ commit นี้เปลี่ยน และไม่ผูก
กับกลไกที่ข้อ C แก้ (ข้อ C แก้แค่การ "แสดงผล" สองจุดให้ sync กัน ไม่ได้แตะจังหวะเซฟ) ไม่ต้องแก้เพิ่ม

**4. ภาพศาลาซูมเห็นด้านใน เดิน/นั่งพัก/เก็บของได้ ทั้ง 1440 และ 390×844 — PASS**
ที่ 1440: เข้าห้องเห็น canvas 814×561 ใช้ `room.crop=[0.1286,0.1844,0.8335,0.5533]` จริง ปุ่ม "นั่งพัก"
และ "เก็บถ้วยน้ำชาร้อน" ปรากฏพร้อมกดได้ (แสดงว่า spawn point `me` อยู่ใน `REACH` ของ `act`/`item`
ตามที่วัดมาใหม่) ย่อ viewport ในหน้าต่างเดิม (ไม่ปิด/เปิดใหม่) ลงเหลือ 390×844 — canvas ปรับขนาดเป็น
360×388 อัตโนมัติ ปุ่มคำสั่งยังอยู่ในจอ (`inViewport:true` ทุกปุ่ม) กดเดินด้วยลูกศรแล้วไม่มี error ใหม่ —
ตรงกับตรรกะใน diff: box คำนวณจาก `sw,sh` (ขนาดที่ครอปแล้ว) ไม่ใช่ขนาดภาพเต็ม จึง scale ตามอัตราส่วน
canvas จริงเสมอ ไม่ผูกกับความกว้างจอตายตัว — สมเหตุสมผลกับโค้ดเดิมของเกมที่ใช้วิธีเดียวกันทุกจุด

โซนเอเชีย (`crop:null`) ตรวจโค้ดแล้ว: `roomFor()` spread `base` ก่อน `zones[zone]` ทับ ดังนั้น
`crop:null` ที่ใส่ไว้จะทับค่า `crop` ของโซนไทยแน่นอน ไม่มีทางสืบทอดค่าผิดไปครอปภาพอื่น — ปลอดภัย

**5. ไม่มี console error ใหม่ · push แล้ว — PASS**
รวบ console error ทั้ง 4 การรัน (trial desktop/mobile, tea desktop) มีแค่ 404 ของ
`img/hero-yama-side.png`, `audio/bgm-title.ogg/.mp3`, `audio/bgm-zone.ogg/.mp3` — ไฟล์เสียง/ภาพสำรอง
ที่หายไปเดิมอยู่แล้ว ไม่เกี่ยวกับไฟล์ที่ batch นี้แตะ (`command-wheel.*`, `ui.js`, `room.js`, `data.js`)
ไม่มี error จาก `BG-Tea.webp` หรือโค้ดที่เพิ่มใหม่เลย ตรงกับที่ Toby รายงาน
Push แล้วจริง — `git log origin/main..HEAD` ว่างเปล่า, `git status` สะอาด (เหลือแค่ `CONCEPT.md`/`files/`/`output/`
ที่ไม่ใช่ของ batch นี้และ Toby ไม่ได้แตะตามข้อห้าม)

## ข้อสังเกตเพิ่มเติม (ไม่ใช่ FIX LIST — แจ้งเผื่อ Chris อยากตรวจต่อ)
- วงคำสั่งฉากต่อสู้ (`.combat-wheel`) ใช้ `bindCommandWheel` เดียวกัน ได้รับการแก้ไปด้วยในตัว (ตรวจโค้ด
  ยืนยันว่าใช้ path เดียวกันจริง) แต่ไม่ได้ทดสอบเจาะจงในรอบนี้ตามที่ Toby ระบุ — เสนอให้ Chris
  ลองเทสต์ฉากต่อสู้เพิ่มเผื่อ regression ถ้ามีเวลา ไม่ใช่ blocker
- ภาพห้องศาลาโซนเอเชีย/ตะวันตก/ไซเบอร์ยังไม่ซูม (ตั้งใจ ไม่อยู่ในสโคป มี `crop:null` กันพังไว้แล้ว)

## สรุป
โค้ดสะอาด ตรงตามที่ commit message อธิบายจริงทุกจุด ไม่มี dead code/console.log ค้าง ไม่แตะไฟล์ต้องห้าม
ทดสอบจริงในเบราว์เซอร์ครบทั้ง 5 เกณฑ์ที่ 1440 และ 390×844 — **PASS ทั้งชุด ไม่มี FIX LIST**
