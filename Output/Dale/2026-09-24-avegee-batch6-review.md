# รีวิว AVEGEE ชุดแก้ที่ 6 (Toby) — Dale

**ใบงาน:** `Output/Claudy/briefs/2026-09-24-avegee-fix-batch6.md`
**รายงาน Toby:** `Output/Toby/2026-09-24-avegee-fix-batch6.md`
**Repo:** `/Users/agapae/Documents/Work PAE/Claude/AVEGEE` — commits `73921df`(B) `60815ff`(E) `c8d9023`(A) `f979ae1`(C) `ba67aa0`(D), push แล้วที่ `main` (`8dd94c8..ba67aa0`)

## วิธีตรวจ

อ่าน diff ทุก commit บรรทัดต่อบรรทัด แล้ว **รันเกมจริงในเบราว์เซอร์จริง (Playwright + Chromium)**
ที่ 1440×900 และ 390×844 — ไม่ใช่แค่คำนวณ CSS เดา: จ้างยมทูต สร้างสถานี ออกหมาย เปิดฉากต่อสู้จริง
ผ่าน `window.G` API (game object ที่ ui.js expose ไว้) + คลิกจริงบน canvas ที่พิกัดในกรอบคลิกจริงของแต่ละสถานี
(`src/data.js STATIONS[].hit`) วัดผลจาก `getBoundingClientRect()` / `getComputedStyle()` / จับเวลาจริงจาก
class `.lunge` ขึ้น-ลง ไม่ใช่จากโค้ดอย่างเดียว

ระหว่างตรวจเจอกับดักทดสอบของตัวเอง 2 จุด (ไม่ใช่บั๊กของ Toby) ที่ทำให้ผลรอบแรกเพี้ยน แก้แล้วตรวจซ้ำ:
- เกมใหม่มีกล่องสอนเล่น ("บทที่ 1 — อ่านสำนวนก่อนตัดสิน") auto-เปิดต่อจากบทนำและ **พักเกมค้างไว้** จนกว่าจะปิดกล่องนั้น — ต้องปิดกล่องสอนก่อนถึงจะทดสอบสถานะ "ไม่ได้พัก" ได้จริง
- กรอบคลิกสถานี (`hit`) กับจุดยืนผู้คุม (`x,y`) เป็นคนละพิกัดกัน คลิกที่ `x,y` ตรงๆ พลาดกรอบคลิกของกระทะทองแดงไปนิดหน่อย (263 อยู่นอก `hit` y-range) ทำให้ dialog ไม่เปิดในรอบแรก

## ผลตรวจแต่ละข้อ

### A — แถบเวลาลงทัณฑ์ไม่ขยับ → PASS
Diff (`src/room.js`, `src/ui.js`) สมเหตุสมผล: ป้ายเดิมที่ชวนกด "ลงมือตรงนี้" ทำอะไรไม่ได้จริง (ฟีเจอร์ถูกถอดไปแล้วเมื่อวาน) ถูกเอาออก เปลี่ยนเป็นบอกสถานะจริง + chip "⏸ เกมพักอยู่" ที่แถบบน

**ทดสอบจริง (Playwright):**
- จ้าง/มอบหมายวิญญาณเข้ากระทะทองแดงจริงผ่าน `g.assign()` แล้วปล่อยเกมเดินจริง (ไม่แตะ pause) — progress ไต่จริง `15.5 → 38.7` ภายใน 2 วิ
- ปิดสถานี กด "⏸ พัก" บนแผนที่ (นอกกล่อง) → `g.paused = true` จริง แล้วเปิดสถานีใหม่ — **chip "เกมพักอยู่" ขึ้นทันที** ตามที่อ้าง
- ระหว่างพัก progress หยุดนิ่งเป๊ะ (`46.37 == 46.37` วัดห่างกัน 1.5 วิ) ไม่ขยับเลย
- ปลดพัก + บังคับ progress ให้ข้าม `need` → วิญญาณออกจากช่อง (`slots.length: 1→0`) และ **มอบหมายวิญญาณดวงถัดไปเข้าช่องที่ว่างได้จริง** (`assign() ok:true`, `free:2`)

**ข้อสังเกต (ไม่ใช่บั๊ก ไม่ต้องแก้โค้ด แต่แก้ขั้นตอนทดสอบในรายงานของ Toby):**
รายงาน Toby ข้อ "สิ่งที่ Kittanate ควรกดตรวจ" บอกให้ "ลองกด ⏸ พัก **ระหว่างอยู่ในหน้าสถานี**" — ทดสอบจริงแล้วพบว่า
ทำแบบนั้นไม่ได้เลยในเกมจริง เพราะกล่องสถานีเปิดด้วย `dlg.showModal()` (native modal) ทำให้ปุ่ม `#play` นอกกล่อง
เป็น inert จริง (Playwright เองก็ actionability-timeout ตอนพยายามคลิกขณะ dialog เปิด) และ `addEventListener('keydown', ...)`
มี guard `if (dlg.open || ...) return;` กันเว้นวรรคไว้ตั้งแต่ต้นด้วย — ผู้เล่นจริงกดปุ่มพัก **ขณะยืนอยู่ในกล่องสถานีไม่ได้**
เส้นทางที่ใช้ได้จริงคือ "กดพักบนแผนที่ก่อน → ค่อยเดินเข้าสถานี" ซึ่งทดสอบแล้วใช้ได้ถูกต้องตามที่อ้าง (chip โผล่ทันทีที่เปิด)
ไม่กระทบเกณฑ์รับงาน ไม่ต้องแก้โค้ด — แค่แจ้งไว้กัน Kittanate งงตอน QA เอง

### B — ปุ่ม "เลือกเป็นปลายทาง" → PASS
`grep -rn "s-pick"` ทั้ง repo ไม่เจอเศษโค้ดเหลือ `pick.st` ยังใช้งานอยู่ผ่านวงคำสั่ง "ที่ไหน" ของห้องสอบสวน (`src/ui.js:1417`) ซึ่งเป็นคนละกลไกที่ diff นี้ไม่ได้แตะ — การลบสะอาด ไม่มี dangling reference

### C — หน่วงตีสวน ~2 วิ → PASS
**วัดเวลาจริงจากคลาส `.lunge`** ผ่านฉากต่อสู้จริง (`g.startDadFight()` เปิดฉากผ่าน `onChange` เดียวกับทุกประเภทการต่อสู้ ยืนยันว่า `openBattle()` ใช้ path เดียวจริงตามที่ Toby อ้าง):
- 1440×900: 2805ms จากคลิกโจมตีถึงเริ่มจังหวะเขาสวนกลับ
- 390×844: 2811ms
ตรงกับ 780(อนิเมชันเรา)+2000(หน่วงใหม่)+overhead เป๊ะ · ปุ่มโจมตี **ยืนยันว่า disabled จริง** ระหว่างรอทั้งสองจอ กดซ้อนไม่ติด

### D — ยมน้อยบัง + ขนาดต่างกัน → PASS (มีข้อยกเว้นที่ยอมรับได้ตามที่ใบงานอนุญาต)
วัด `getBoundingClientRect()` จริงในฉากต่อสู้ (จัดทีมยมทูต 2 คน แล้วเข้าสู้จริง) ทั้งสองจอ:
- ยมน้อย `z-index:4` > ทีมยมทูต `z-index:3` เสมอ — **ไม่มีตัวไหนบังหัวยมน้อยได้อีก**
- กรอบภาพยมน้อยกับทีมยมทูต **ไม่ทับกันเลยทั้งสองจอ** (คำนวณ overlap จาก rect จริง = false ทุกคู่)
- อัตราส่วนสูง ยมทูต/ยมน้อย = 71.2% (1440) / 73.7% (390) — ต่ำกว่าเป้า "≤15% ต่าง" (คือ ≥85%) ที่ใบงานขอ
  ตรงกับที่ Toby รายงานตรงไปตรงมาว่าไม่ถึงเป้า พร้อมเหตุผล (พื้นที่ว่างฝั่งซ้ายของยมน้อยในโหมดต่อสู้แคบกว่าที่คิด
  ต้องขยับผังวงคำสั่งทั้งฉากถึงจะดันได้มากกว่านี้ — งานคนละขอบเขต) — ใบงานอนุญาตให้ระบุเป็นข้อยกเว้นได้ถ้าปรับ
  ในไม่กี่บรรทัดไม่ได้จริง ยอมรับตามที่ Toby เสนอ เพราะเกณฑ์หลัก (เห็นครบ 3 ตัว ไม่ทับกัน ไม่บังการ์ด/ป้าย) ผ่านจริง
  ทั้งสองจอ — ควรเปิดใบงานแยกถ้าต้องการดันเข้าใกล้ 85% จริงๆ

### E — แถบผู้คุมที่ศาลาน้ำชา → PASS
ทดสอบจริง: เปิดกระทะทองแดง (cap>0) เห็น h4 "ผู้คุมประจำหลังนี้" · สร้างศาลาน้ำชาใหม่จริงผ่าน `g.build('tea')` รอสร้างเสร็จจริง (build timer นับถึง 0) แล้วเปิดดู — **ไม่มี h4 นั้นเลย** ตรงตามที่อ้าง

### ข้อ 6 — ไม่มี console error ใหม่ → PASS
ไม่มี `pageerror` ใดๆ ตลอดการทดสอบทุกฉาก (สถานี/ต่อสู้/สร้างอาคาร) มีแค่ 404 ที่มีอยู่ก่อนแล้วและไม่เกี่ยวกับ diff นี้ (`img/hero-yama-side.png`, `audio/bgm-title.ogg`, `audio/bgm-zone.ogg` — ไฟล์ art/เสียงที่ยังไม่มี โค้ดมี fallback ตามคอมเมนต์เดิมอยู่แล้ว)

### ข้อห้าม
`git status` สะอาด — ไม่มี commit แตะ `src/cases.js`/`src/cases-asia.js`/`CONCEPT.md`/`files/`/`output/` ตามที่ Toby รายงาน ยืนยันแล้ว

## สรุป

**PASS** — ทั้ง 6 เกณฑ์รับงานผ่านจริงจากการทดสอบในเบราว์เซอร์จริง ไม่ใช่แค่จากโค้ด ไม่มีข้อที่ต้องแก้เพิ่ม
ข้อสังเกตเดียว (ขั้นตอน "กด ⏸ ระหว่างอยู่ในหน้าสถานี" ในรายงาน Toby ทำไม่ได้จริงเพราะ modal บล็อก) เป็นเรื่องเอกสารทดสอบ
ไม่ใช่บั๊ก ไม่ต้องแก้โค้ด — แจ้งไว้เผื่อ Kittanate ทำตามขั้นตอนนั้นแล้วงงว่าทำไมกดปุ่มพักไม่ติด

## Live URL / วิธี verify เอง
- `https://hellopae.github.io/AVEGEE/` (รอ Pages deploy อัตโนมัติจาก push ที่ Toby ทำไปแล้ว)
- Local: `cd "/Users/agapae/Documents/Work PAE/Claude/AVEGEE" && python3 -m http.server 8765` แล้วเปิด `http://localhost:8765/index.html`
- ขั้นตอน QA แนะนำ (แก้จากของ Toby ตรงข้อ A): กด "⏸ พัก" **ตอนยืนอยู่บนแผนที่** (ก่อนเดินเข้าสถานี) แล้วค่อยเดินเข้ากระทะทองแดง — จะเห็น chip "⏸ เกมพักอยู่" ทันที

## Rollback
ถ้าต้องย้อน: `git revert ba67aa0 f979ae1 c8d9023 60815ff 73921df` (เรียงจากล่าสุดขึ้นไป) แล้ว push — ทุก commit แยกข้อสมบูรณ์ ย้อนทีละข้อได้โดยไม่กระทบข้ออื่น
