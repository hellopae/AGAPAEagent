# รีวิว AVEGEE ชุดแก้ที่ 7 (Toby) — Dale

**ใบงาน:** `Output/Claudy/briefs/2026-09-24-avegee-fix-batch7.md`
**รายงาน Toby:** `Output/Toby/2026-09-24-avegee-fix-batch7.md`
**Repo:** `/Users/agapae/Documents/Work PAE/Claude/AVEGEE` — commits `6b5cd1c`(B) `acf9487`(D) `2cfe419`(C) `9e9637c`(A), push แล้วที่ `main` (`ba67aa0..9e9637c`)

⚠️ Toby กำลังทำชุดที่ 8 ในโฟลเดอร์ repo เดียวกันพร้อมกันตอนตรวจ (เห็น `CONCEPT.md`/`src/*.js` แก้ค้างอยู่ ไม่ commit)
— **ไม่แตะ working tree หลักเลย** ใช้ `git worktree add --detach /tmp/dale-review/avegee-batch7 9e9637c` แยกไปทดสอบ
แล้ว `git worktree remove --force` ทิ้งหลังตรวจเสร็จ ยืนยัน `git worktree list` เหลือแค่ main ก่อนปิดงาน

## วิธีตรวจ

อ่าน diff ทุก commit ก่อน แล้ว **รันเกมจริงในเบราว์เซอร์จริง (Playwright + Chromium)** ที่ 1440×900 และ 390×844
ผ่าน `python3 -m http.server 8799` บน worktree ที่ล็อกไว้ที่ commit `9e9637c` — ไม่ใช่แค่อ่านโค้ดแล้วเชื่อ
ใช้ `window.G` (game object ที่ ui.js expose) ตั้งค่าฉาก (สร้างสถานี/ปลดล็อกเลเวล/เติมเบี้ยกรรม) ให้ไปถึงจุดที่ต้องตรวจเร็วขึ้น
แต่ **การกดปุ่ม/อ่านผลลัพธ์ทุกข้อทำผ่าน DOM จริง** (คลิกปุ่มจริง, เดินตัวละครในห้องจริงด้วยคีย์บอร์ดจริงผ่าน `window.__room.tick()`,
วัดเวลาจริงด้วย `Date.now()`/`performance` จนกว่าข้อความ "เขาสวนกลับ" ขึ้นจริงบนจอ) ไม่ได้ข้ามด้วยการเรียกฟังก์ชันเปล่าๆ ยกเว้นจุดที่ระบุไว้ชัดเจนด้านล่าง

**กับดักที่เจอระหว่างตรวจ (ไม่ใช่บั๊ก Toby):**
- เมนูวงคำสั่ง ("พลัง"/"ที่ไหน"/ฯลฯ) เป็นเรดial menu ต้องคลิก segment เปิดกลุ่มก่อนถึงจะกดตัวเลือกข้างในได้ — Playwright มองว่าปุ่มกลาง "ออกหมาย" ทับ hit-area จนคลิกจริงไม่ติด ต้อง dispatch คลิกผ่าน `element.click()` ตรงๆ ใน `evaluate()` แทน `page.mouse.click()`
- ที่ 390px กว้าง แถบสถิติ HUD (chip การ์ด) ลอยทับส่วนบนของ canvas แผนที่จริง (ดีไซน์เดิม ไม่เกี่ยวกับชุดนี้) ทำให้คลิกที่พิกัดสถานีบาง path โดน chip แทน — แก้โดย dispatch `MouseEvent('click')` ตรงบน `#cv` (คำนวณ `clientX/Y` เอง) แทนคลิกจริงผ่าน OS ซึ่งเลี่ยง hit-test ของ browser ที่โดนของลอยทับ — ยืนยันว่าปุ่มที่ตรวจ "มีอยู่จริงและกดได้" ผ่าน DOM ปกติ ไม่ใช่ปัญหาของ batch นี้

## ผลตรวจแต่ละข้อ

### 1 — ตวาดข่มขู่: cooldown 2 นาทีเวลาจริง → PASS
- `g.powerReady('roar')` = true ตอนเริ่ม (`readyAt:0`) → กดผ่านวงคำสั่งจริง (คลิก `[data-pw="roar"]`) → `readyAt` เดินหน้าไปเป็น `Date.now()+119694ms` (คลาดเคลื่อนจาก 120000 แค่เวลาที่โค้ดรันจริง ตรงตามที่ควร)
- ปุ่มขึ้น badge/title `"รออีก 2:00"` ทันที และ **นับถอยหลังจริงบนจอ** (2:00 → 1:57 หลัง 2.5 วิ ผ่าน `setInterval` ของ ui.js เอง ไม่ใช่ mock)
- เซฟจริง (`g.save()`) แล้ว **`page.reload()` จริง** (โหลดหน้าใหม่ทั้งหมด กด "เล่นต่อ") — `readyAt` เท่าเดิมเป๊ะ ยังติด cooldown ถูกต้อง ไม่รีเซ็ตเป็นพร้อมใช้
- ลูกไฟในฉากต่อสู้: เปิดฉากสู้เปรตจริงผ่าน `#atk`→"เข้าต่อสู้" แล้วกด `battleAct('fire')` จริง → `fireAmmo` ลด 3→2 โดย `roar.readyAt` **ไม่ขยับเลย** ยืนยันแยกสระกระสุนจริงตามที่อ้าง

### 2 — กระจกวิเศษ: เฉลยที่ไหน+ความแรงถูกต้อง + ได้ item จากแผนที่/กานต์ → PASS
- `usePower('mirror', soul)` จริงกับดวงในคิว → ข้อความที่ขึ้นจริงคือ `"กระจกส่องเห็นที่ที่ควรลงทัณฑ์: 🍳 กระทะทองแดง (ตรงกรรม ฉ้อโกง)"` + `"...ความแรงที่พอดี: ระดับ 5 — มหันต์"`
  เทียบกับคำนวณอิสระ (ไม่ได้อ่านจากโค้ดพลังเอง) จากสูตรเดียวกับ `judge()` ว่าสถานีไหนน้ำหนักบาปตรงที่สุด — **ตรงกันเป๊ะ** ทั้งชื่อสถานีและตัวเลขความแรง
- เดินเข้าห้องหอส่องกรรมจริง (คลิก hitbox สถานีจริงบน canvas) เห็น **กานต์ยืนอยู่ในฉากจริง** (ใช้ภาพ crew-kan เดิม) ปุ่ม "คุยกับกานต์" มี hint ถูกต้องตามสถานะ (เต็มแล้ว/ล็อก/คูลดาวน์เท่าไหร่)
- ดูดกระจกให้เหลือ 0 แล้วกด "คุยกับกานต์" จริง → ammo 0→1, กดซ้ำทันทีถูกบล็อก (`talkKan()` คืน false, ปุ่มโชว์ "อีก 5 วาระ") ตรงตาม `kanCool:5`

### 3 — สะกดจิต: สารภาพ 100% + ซื้อจากบุญ → PASS
- `buyBoon('hypno')` จริง → หัก 155 เบี้ยกรรมเป๊ะ, เข้า `inventory.hypno`
- `useBag('hypno')` จริง → ย้ายเข้า power ammo ถูกต้อง
- ตั้งดวงทดสอบให้มีกรรมซ่อน + ของอ้างเท็จ แล้ว `usePower('hypno', soul)` จริง → **deeds ทุกชิ้น known=true, merits ปลอมทุกชิ้น exposed=true** (100% ตามที่อ้าง)

### 4 — ไม่มีปุ่มเพิ่มช่องรับ/ประหยัดฟืน + เซฟเก่าไม่พัง → PASS
- เปิดแผงสถานี (กระทะทองแดง) จริงทั้ง 1440 และ 390 — ปุ่มในแผง "ทำอะไรได้ตรงนี้" เหลือแค่ `"⚙️ เร่งการทำงาน ขั้น 0"` ปุ่มเดียว ไม่มี "เพิ่มช่องรับ"/"ประหยัดฟืน" เลย
- จำลองเซฟเก่า (`capLv:3`) แล้วเรียก `stCap()` จริง — ความจุเพิ่มขึ้นจริง (3→6) แม้ไม่มีปุ่มกดอัปเกรดแล้ว
- **ทดสอบเพิ่ม (นอกเกณฑ์แต่สำคัญ):** จำลอง save object ทรง v2 แบบเก่าจริงๆ (ไม่มี `fireAmmo`/`readyAt`/`kanCd` เลยสักฟิลด์) แล้วเรียก `g.restore()` ตรงๆ — **ไม่โยน exception**, `fireAmmo` ตกดีฟอลต์ 3/3, `roar.readyAt` ตกดีฟอลต์ 0 (ใช้ได้ทันที), `capLv` เดิมยังอยู่ครบ — โหลดเซฟเก่าสุดๆ ก็ไม่พัง

### 5 — ยมน้อย+ยมทูตหันขวาทุกท่า + ตีกลับเร็วขึ้น → PASS
- อ่าน CSS จริงใน `index.html`/`command-wheel.css` ยืนยันตรงกับที่ Toby อธิบาย: `.fig.you img{scaleX(-1)}` (ท่ายืน, สมมาตรไม่กระทบ) ถูกยกเว้นเฉพาะ `.fig.you.atk img{scaleX(1)}` (ท่าโจมตี ใช้ภาพที่หันขวาอยู่แล้ว) และ `.battle-squad img{scaleX(-1)}` (ยมทูตช่วย ไม่เคยมีมาก่อน)
- เปิดฉากต่อสู้จริง (จ้างสู้เปรตผ่าน `#atk`→"เข้าต่อสู้"→"โจมตี") ถ่าย screenshot จริงทั้ง 1440/390: **ยมน้อยหันขวาเข้าหาศัตรูชัดเจนตอนโจมตี** ตัวหนังสือ/ป้ายชื่อไม่กลับด้าน
- คัตซีนพลัง (กดตวาดข่มขู่จริง) ยืนยันจากภาพจริง: ลูกไฟ/มือปล่อยพลังพุ่งไปทาง**ขวา**ตรงกับทิศศัตรู (`actionRush` keyframe คง `scaleX(-1)` ตลอดแอนิเมชัน ไม่มีจังหวะที่หลุดกลับไปซ้าย)
- วัดเวลาจริงจากคลิก "โจมตี" จนข้อความ "เขาสวนกลับ" ปรากฏบนจอ (poll ทุก 50ms ไม่ใช่คำนวณจากโค้ด) ได้ **1630ms** (รันซ้ำอีกครั้งด้วยสคริปต์คนละตัวได้ 1678ms) — ตรงกับเป้า ~1580ms ที่ Toby อ้าง ไม่รู้สึกค้าง ปุ่มโจมตี disabled จริงระหว่างรอ

### 6 — ไม่มี console error ใหม่ + push แล้ว → PASS
`pageerror` = ไม่มีเลยตลอดการทดสอบทุกสคริปต์ (ทั้งใน dialog หลายชั้น, ฉากสู้, เดินในห้อง) มีแค่ 404 ที่มีอยู่ก่อนแล้วไม่เกี่ยวกับ diff นี้ (asset ที่ยังไม่มีไฟล์ตามที่ Toby ระบุ) — `git log` ยืนยัน push ถึง `9e9637c` แล้วจริง

### 7 — ตรวจกรรมกับบุญที่ประตูสวรรค์ (ข้อ D) → PASS
- ตั้งวิญญาณทดสอบเข้าคิว "รอที่ประตู" จริง แล้วเดินเข้าห้องประตูสวรรค์ — **ตอนยืนไกล ปุ่ม "ให้บุญตรวจกรรม" disabled พร้อม hint ชัดเจนบนจอ** `"⚠️ เดินเข้าไปยืนใกล้บุญในห้องก่อน ปุ่มถึงจะกดได้"` (ไม่ใช่ปุ่มเงียบๆ แบบเดิม)
- **เดินจริงในห้องด้วยคีย์บอร์ด/คลิกจริงผ่าน `window.__room.tick()`** (ไม่ได้ข้าม) จนเข้า `inReach()` จริง → ปุ่มเปิดใช้งานเอง
- กดปุ่มจริงบน DOM: "ให้บุญตรวจกรรม" → "ส่งไปเกิดใหม่" ครบสองขั้น → **ตัวนับ `reborn` เพิ่มจริง 0→1**, วิญญาณหลุดออกจาก `sentences` (ช่องว่างรับดวงถัดไปได้), แผงอัปเดตเป็น "รอที่ประตู 0 ดวง" ทันที
- ปุ่ม "ซื้อวงสะกดจิตจากบุญ" อยู่แยกคนละปุ่มใน DOM คนละ handler (`data-buy-boon` vs `data-gate-check`) เห็นทั้งคู่พร้อมกันในแผงเดียว ไม่ชนกัน ตรงตามเกณฑ์

## ทดสอบเพิ่มที่ 390×844 (ละเอียดทุกปุ่มใหม่ ตามที่คุณเป้สั่ง)
ไล่คลิกซ้ำทุกปุ่มใหม่ของ batch นี้ที่ 390px แยกจาก 1440px (ไม่ได้เดาจาก responsive CSS อย่างเดียว):
- แผงสถานี (กระทะทองแดง/หอส่องกรรม/ประตูสวรรค์) — ไม่มี horizontal overflow (`scrollWidth <= clientWidth` ทุกกล่อง), ปุ่มใหม่ทุกปุ่มมองเห็นและกดได้จริง, ข้อความ hint ไม่ตัดกลางคำ
- วงคำสั่งพลัง — ปุ่ม "ตวาดข่มขู่" กดได้จริง โชว์ countdown `2:00` อ่านออกชัดเจน
- ฉากต่อสู้ — ท่าโจมตียมน้อยหันขวาถูกทางเหมือน 1440, กล่องไม่ล้นขอบจอ
- ไม่มี console error ใหม่ที่ 390px เช่นกัน

## ข้อห้าม
ตรวจ diff ทุก commit แล้ว: ไม่มีการแตะ `src/cases.js`/`src/cases-asia.js`/`CONCEPT.md`/`files/`/`output/` เลย, ไม่เปลี่ยนกลไกคะแนนตัดสิน (นอกจากผลพลัง 3 ท่าตามข้อ A), ไม่แตะระบบฟืน — ครบตามที่ Toby รายงาน

## สรุป

**PASS** — ทั้ง 7 เกณฑ์รับงานผ่านจริงจากการทดสอบในเบราว์เซอร์จริงทั้ง 1440×900 และ 390×844 (Playwright + Chromium)
ไม่ใช่แค่อ่านโค้ดแล้วเชื่อ — ทุกข้อมีการกดปุ่มจริง/วัดเวลาจริง/เดินตัวละครจริงในห้อง ยกเว้นบางจุดที่ระบุชัดเจนว่าใช้ `usePower()`/`buyBoon()` ตรงๆ
แทนการคลิก UI ซ้ำ (เพราะ UI นั้นเรียกฟังก์ชันเดียวกันอยู่แล้ว และได้ยืนยัน UI wiring แยกต่างหากในข้อ 2/3/7)
ไม่มีข้อที่ต้องแก้เพิ่ม ไม่มี FIX LIST

## Live URL / วิธี verify เอง
- `https://hellopae.github.io/AVEGEE/` (รอ Pages deploy อัตโนมัติจาก push ที่ Toby ทำไปแล้ว)
- Local: `cd "/Users/agapae/Documents/Work PAE/Claude/AVEGEE" && python3 -m http.server 8765` แล้วเปิด `http://localhost:8765/index.html`
- ขั้นตอน QA ตามที่ Toby ระบุในรายงานใช้ได้ตรงจริง 6 ข้อ — เพิ่มเติมสำหรับ Chris/คุณเป้:
  ลองปิดเกม/เปิดใหม่ระหว่างที่ตวาดข่มขู่ยังติดคูลดาวน์อยู่ — เวลาที่เหลือต้องนับต่อถูก ไม่รีเซ็ต (ทดสอบแล้วผ่านจริง)

## Rollback
ถ้าต้องย้อน: `git revert 9e9637c 2cfe419 acf9487 6b5cd1c` (เรียงจากล่าสุดขึ้นไป) แล้ว push — ทุก commit แยกข้อสมบูรณ์ ย้อนทีละข้อได้โดยไม่กระทบข้ออื่น
