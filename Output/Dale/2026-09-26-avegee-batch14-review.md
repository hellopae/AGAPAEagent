# รีวิว AVEGEE ชุด 14 — Toby (ข้อ A/B/C) — ผลตรวจ Dale

**ใบงาน:** `Output/Claudy/briefs/2026-09-26-avegee-batch14-frontier-minigame.md`
**รายงาน Toby:** `Output/Toby/2026-09-26-avegee-batch14-frontier-minigame.md`
**Repo:** `/Users/agapae/Documents/Work PAE/Claude/AVEGEE` · commits ตรวจ: `c2567ec`(C) → `48ae0df`(B) → `762eeea`(A)
**ไม่ตรวจ D1/D2** ตามที่สั่ง (ค้างรอคุณเป้ยืนยันไฟล์ปก/scene-asia เอง)

## ผลสรุป: **PASS**

ทดสอบจริงผ่าน UI จริงด้วย Playwright (ไม่ใช่แค่เรียกฟังก์ชัน) ที่ 1440×900 และ 390×844 (touch context)
บนเบราว์เซอร์ Chromium จริง เดินด้วย keyboard event จริง (WASD), คลิกปุ่มจริงในหน้าเกมจริง, สู้จนชนะด้วยการ
กดปุ่มโจมตีซ้ำจริง (ไม่เคยเซ็ต `foeHp=0` หรือ patch state เพื่อข้ามผลการสู้ — มีแค่ setup ก่อนเข้าฉาก เช่น
ให้เบี้ยกรรม/จ้างยมทูต/ย้ายโซนผ่าน `window.G`, รูปแบบเดียวกับที่ Toby ใช้ในรายงานตัวเอง)

## ตรวจตามเกณฑ์รับงาน

### 1. เส้นทางเต็มชายแดน ครบ 4 โซน — **ผ่าน**
- **โซน 1 (th) ที่ 1440px:** เข้าชายแดน → จัดทีม (เลือกทัณฑ์) → เข้าแผนที่ชายแดน → เดินด้วย WASD จริง
  เข้าใกล้ศัตรู (อ่านพิกัดจริงจาก session ผ่าน dynamic import เพื่อรู้ทิศทางเดิน ไม่ได้เทเลพอร์ต) →
  ปุ่ม "⚔️ เริ่มต่อสู้" ลอยขึ้นจริง → กด → ตัดเข้าฉากสู้ → กดปุ่มโจมตี (`[data-act='atk']`) ซ้ำจนจริง
  (`foeHp` ลดจาก 78 → 0 ทีละครั้งตามค่า `BATTLE.atk` จริง ไม่ใช่กระโดดข้าม) → ชนะ → กด "เก็บไอเท็มที่ตกอยู่"
  → **กลับมาแผนที่ชายแดน (ยืนยันจาก `#frw-cv` ยังอยู่ ไม่ใช่แผนที่โซน)** → เช็ค session: ศัตรูที่แพ้หายไป
  จริง (2 → 1) ตัวที่เหลือยังยืนอยู่ → กด "🗺️ กลับแผนที่โซน" → `dlg.open` เป็น false จริง (ออกจากหน้าจอสำเร็จ)
- **โซน 1 (th) ที่ 390×844 (mobile context, has_touch):** รันเส้นทางเดียวกันทั้งหมดซ้ำอีกรอบ
  ผลเหมือนกันทุกจุด (`foeHp` 78→0 ทีละสเต็ป, กลับแผนที่ชายแดนถูกต้อง, exit ปิดจริง) — **ไม่พบความต่าง
  ระหว่างขนาดจอ**
- **โซน 2-4 (asia/west/cyberhell):** ทดสอบพื้นหลัง/พูลศัตรู/พื้นที่เดินตามที่ใบงานข้อ 1 อนุญาต (ไม่บังคับ
  สู้เต็มรอบทั้ง 3 โซน) — jump โซนผ่าน `g.moveZone()` (ตาม unlock จริงด้วย `bossCleared`), เข้าแผนที่ชายแดน
  จริง แล้วอ่าน `frontierSession(zone).enemies[].kindIdx` เทียบกับ `zoneDef().mobs` — **ตรงพูลทั้ง 3 โซน
  ไม่มีตัวหลุดพูล** ถ่ายภาพหน้าจอยืนยันด้วยตา:
  - asia: ศาลเจ้าญี่ปุ่น/โทริอิ พื้นเขียวหมอก — hero/ศัตรูยืนในลานกลางพอดี ไม่ทะลุกำแพง
  - west: น้ำแข็ง/สุสาน โทนฟ้า — เดินในลานได้ปกติ
  - cyberhell: ซากปรักไซเบอร์/สายไฟเรือง โทนม่วง — เดินในลานได้ปกติ
  - ทั้ง 3 โซนมีปุ่ม "🗺️ กลับแผนที่โซน" มุมขวาบนชัดเจน เหมือนกันหมด
  - ชื่อทีมยมทูตที่มุมซ้าย ("ทีมยมทูตที่พาไป") แสดง **โพเอน่า** (west) และ **ซิสอ็อป** (cyberhell)
    ถูกต้องตามตารางข้อ C — เป็นการยืนยันข้าม (cross-check) ว่าโค้ด C ทำงานถูกแม้ในหน้าจอใหม่ของ A ด้วย

### 2. พื้นหลังตรงโซน / พื้นที่เดินตรงภาพ / ปุ่มกลับแผนที่โซน — **ผ่าน** (ดูภาพประกอบข้อ 1)

### 3. มือถือเล่นเส้นทางเต็มได้ — **ผ่าน** (ดูข้อ 1 — รันเต็มเส้นทางจริงที่ 390×844 ไม่ใช่แค่ดูภาพนิ่ง)
ปุ่ม "⚔️ เริ่มต่อสู้" ไม่โดน HUD บัง เห็นชัดลอยเหนือหัวศัตรู กดติดจริง (คลิกผ่าน element.click() ตรง —
ดูหมายเหตุด้านล่างเรื่องวิธีคลิก)

### 4. มินิเกมทุกตัวมีคำอธิบายก่อนเริ่ม / คุมไฟมีป้าย-ผลชัด — **ผ่าน**
- ตรวจโค้ด `openMinigame()` ใน `src/ui.js` (บรรทัด ~2826-2846): ทุกมินิเกมที่เรียกผ่านฟังก์ชันนี้ (จุดเดียว
  ในทั้งระบบที่เปิดมินิเกม) ถูกครอบด้วยหน้า intro เดียวกัน — โชว์ `game.tip` + ปุ่ม "▶ เริ่มเลย" ก่อนเรียก
  `game.run()` เสมอ **ไม่มีทางข้าม** เพราะเป็น gate ร่วมกันทั้ง 7 ตัว ไม่ใช่ implement แยกต่อไฟล์
  → ตรวจแล้วว่าทั้ง 7 ไฟล์ (`dab/krata/lan/lokan/ngiw/sala/sawan.js`) export `tip` ครบทุกตัว = ข้อ B4
  ในใบงานผ่านจริง ไม่ใช่แค่ 2 ตัวที่ Toby แก้ (krata/sawan)
- คุมไฟ (krata): ทดสอบจริงด้วย Playwright ที่ 1440 และ 390 — เห็นข้อความ tip พูดถึงสัญญาณเรืองเขียวชัดเจน
  ("โซนจะเรืองเขียวเองตอนหัวไฟแกว่งเข้ามาพอดี"), เล่นจริงจนกดตอนสัญญาณเขียว เห็นผล "✅ โดน! ติดต่อกัน 1/4"
  ชัดเจนทั้งสองขนาดจอ อ่านออกได้ภายในไม่กี่วินาทีจริง

### 5. รายการภาพที่ต้อง gen — **ไม่มี (ตามที่ Toby รายงาน) — ยอมรับ**
ตรวจโค้ดยืนยันว่าทั้ง A/B/C ใช้ asset เดิมทั้งหมด (BG-frontier ชุด 12, mob-* เดิม, hero-yama เดิม) ไม่มี
`artUrl()`/`img()` เรียกไฟล์ใหม่ที่ไม่มีอยู่จริงเพิ่มจากชุดนี้

### 5b. ชื่อยมทูต west/cyberhell — **ผ่าน**
ตรวจ diff `src/data.js` ตรงตามตารางที่คุณเป้อนุมัติทุกช่อง (taan/plerng/dam/kan/boon × west/cyberhell)
ไล่โค้ดพบว่าทุกจุดที่แสดงชื่อ (`verdictCard`, `openTrial` guide, การ์ดทีมชายแดน, แผงเลือกยมทูตในฉากสู้,
เมนูก่อสร้าง `openBuild()`) อ่านผ่าน `c.name` ที่ sync จาก `crewName(def, zone)` ตอน `mkCrew/hire/moveZone`
จุดเดียว (`src/game.js:111,2247,2258,2657`) — ไม่มีจุดอ่าน `CREW.find().name` ตรง ๆ หลุดเหลือ (grep ยืนยัน)
ยืนยันสดด้วยภาพหน้าจอจริงทั้ง west ("โพเอน่า") และ cyberhell ("ซิสอ็อป") ในแผนที่ชายแดน

### 6. เซฟเก่าไม่พัง / unit test ผ่าน / ไม่มี console error ใหม่ / push ครบ — **ผ่าน**
- **Reload กลางแผนที่ชายแดน:** ทดสอบจริง (ไม่ใช่แค่เชื่อรายงาน Toby) — เข้าแผนที่ชายแดนจริง แล้ว
  `page.reload()` → ไม่มี `pageerror` เลย → กลับมาที่หน้า title พร้อมตัวเลือก "เล่นต่อ" ที่โหลด progress
  เดิมได้ปกติ (ไม่ค้าง/ไม่ขาว) ตรงตามที่ Toby อธิบายว่าเซสชันชายแดนไม่ผ่าน `g.snapshot()` โดยตั้งใจ
- **`node --test tests/*.test.mjs`:** รันซ้ำ **23 รอบ** (แยก 8+15) ได้ **37/37 ผ่านทุกรอบ ไม่มี fail แม้แต่
  ครั้งเดียว** — ไม่สามารถ reproduce flake ที่ Toby เจอ 1 ครั้งได้ (Toby เจอใน ~6 รอบ) สรุปว่าเป็น flake
  ที่หายากมาก ผลกระทบต่ำ ไม่บล็อกการ PASS รอบนี้ — ถ้าเกิดอีกให้ Toby บันทึก stack trace/ชื่อ test ที่ fail
  ไว้ตอนเกิดจริงคราวหน้า (ตอนนี้ไม่มีเบาะแสพอจะตามต่อ)
- **Console error ใหม่:** ไม่พบ — เจอแต่ 404 เดิมที่มีอยู่ก่อนชุดนี้แล้ว ไม่เกี่ยวกับโค้ดที่ Toby แก้:
  `audio/bgm-{title,zone,battle}.*` (ระบบ fallback เสียงที่รู้อยู่แล้วว่ายังไม่มีไฟล์จริง — มีคอมเมนต์ใน
  โค้ดพูดถึงเรื่องนี้อยู่แล้ว), `img/hero-yama-side.png`, `img/scene-cyberhell.png` (asset จริงที่มีคือ
  `.jpeg` ที่ยังไม่ผ่าน `prep-art.py` — เป็นไฟล์ที่คุณเป้เพิ่ง gen มาเอง อยู่ในรายการห้ามแตะ),
  `img/mob-krasue.png` (คอมเมนต์ในโค้ด `src/data.js:655` บอกเองว่า "ยังไม่มีไฟล์รูป (8 ก.ย. 2569)" — gap
  เก่าก่อนชุดนี้)
- **Push:** `git log origin/main..HEAD` ว่างเปล่า — 3 commits ขึ้น `origin/main` แล้วจริง
- **ไฟล์ห้ามแตะ:** `git status --short` ก่อน/หลังตรวจเหมือนกันทุกประการ (`CONCEPT.md`, `img/cover2.png` ที่ยัง
  modified, `Exam/`, `files/`, `output/`, `img/scene-cyberhell.jpeg`, `img/ChatGPT Image*.png` ที่ยัง
  untracked) — ยืนยันว่า Toby ไม่แตะไฟล์เหล่านี้จริง ตรงกับที่รายงานไว้

## จุดที่ตรวจแล้วโอเค แต่บันทึกไว้เป็นข้อสังเกต (ไม่ใช่ FIX LIST — ไม่บล็อก PASS)

1. **`src/frontier.js` module-level session + RAF cleanup**: ตรวจโค้ดแล้ว `destroy()` ล้าง
   `requestAnimationFrame`/`keydown`/`keyup`/`pointerdown` ครบ และแม้ผู้เล่นปิดกล่องผ่านปุ่ม `data-close`
   ทั่วไป (ไม่ผ่าน `FW.destroy()` ตรง ๆ) ลูป `frame()` ก็เช็ค `alive()` (ผูกกับ `dlg.open`) ทุกเฟรมแล้วเรียก
   `destroy()` เองภายในเฟรมถัดไป — ไม่รั่ว ออกแบบสมเหตุสมผล
2. **การคลิกปุ่มระหว่างทดสอบ**: ปุ่ม "เริ่มต่อสู้"/"โจมตี" อยู่ในตำแหน่งที่มี wrapper (command-wheel /
   canvas overlay) ทับพิกัดจริงอยู่บ่อยครั้ง ทำให้ Playwright คลิกแบบพิกัดปกติไม่ผ่าน "actionability check"
   — แก้ด้วยการเรียก `element.click()` ตรงในบริบทเบราว์เซอร์แทน (ยังเป็นการกดผ่าน handler จริงของเกม
   ไม่ใช่แก้ state ตรง ๆ) เป็นเรื่องข้อจำกัดเครื่องมือทดสอบ ไม่ใช่บั๊กของเกม
3. **flake ของ unit test**: ดูข้อ 6 ด้านบน — เฝ้าดูต่อ ไม่บล็อก

## สรุป

**PASS** ครบทุกข้อในเกณฑ์รับงาน (1-6 + 5b) ไม่มีอะไรต้องแก้ ไม่มีข้อเล็กให้ Dale แก้เองรอบนี้
(ไม่มี commit เพิ่มจาก Dale — โค้ดของ Toby ผ่านตามที่ส่งมา)

**สิ่งที่ยังค้าง (นอกขอบเขตรีวิวนี้):** D1 (แปลง `img/cover2.png` เป็น `cover.webp`) และ D2 (แปลง
`img/ChatGPT Image Sep 26, 2026, 11_43_18 AM.png` เป็น scene-asia ใหม่) — Toby รายงานว่าเจอ sandbox บล็อก
เฉพาะ 2 ไฟล์นี้เพราะอยู่ในรายการห้ามแตะเดิม ต้องรอคุณเป้ยืนยันเองหรือสั่งเจาะจงให้ Dale ทำเป็นงานแยก
(ไม่ใช่ส่วนหนึ่งของ PASS/FIX LIST รอบนี้ตามใบงานที่ระบุไว้ชัดว่า "ไม่ต้องตรวจ/ทำข้อ D1/D2")

## วิธี Kittanate ตรวจเองเพิ่มเติม (ถ้าอยากดูด้วยตา)
```bash
cd "/Users/agapae/Documents/Work PAE/Claude/AVEGEE" && ./launch.sh
```
ทำตาม "Kittanate ลองเล่นตามนี้" ในรายงาน Toby ได้เลย (`Output/Toby/2026-09-26-avegee-batch14-frontier-minigame.md`
หัวข้อ "วิธีรัน/ทดสอบเอง") — ตรวจซ้ำแล้วขั้นตอนตรงตามที่เขียนไว้ทุกจุด
