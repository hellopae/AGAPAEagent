# AVEGEE — Layout ใหม่หน้าสอบสวน+ต่อสู้ (สเปก Vera) + Part H (ภาพโซน 3/4)

**Repo:** `/Users/agapae/Documents/Work PAE/Claude/AVEGEE` · commit `2ac2dd4..e44550a` (4 commit) · push แล้ว
**สเปกอ้างอิง:** `Output/Vera/2026-09-18-avegee-trial-battle-ui.md`

## สิ่งที่ build

### 1. หน้าต่อสู้ (commit `2ac2dd4`)
- token `--hell-purple-1/-2` ใหม่ใน `:root`
- `.corner-tick` 4 มุม (ใหม่) + กรอบทอง 1.5px + double-line inset shadow รอบ `.arena`
- `.arena:before` overlay ผสมม่วงแทนดำล้วนที่ขอบบน
- `.plate` (ป้ายชื่อ+หลอดเลือด) ไล่สีม่วง ขอบทอง แทนพื้นทึบเทา
- `.talkbox` กรอบทองบางแทน `var(--border)` เทา
- `.hud-btn` คอมโพเนนต์ปุ่มใหม่ — ผูกกับ `.acts button` ผ่าน selector รวม ไม่ต้องแก้ template
  ปุ่มฉากต่อสู้เลย (⚔️ฟาด/🔥ลูกไฟ/ไอเทม/เรียกยมทูตได้สไตล์ชุดเดียวกันทันที)
- ตัวละครขยาย: `dialog.rpg` 940→1020px desktop, `.arena .fig img` clamp 78-148→140-215px
  desktop, เพิ่ม breakpoint ≤600px แยก clamp 105-165px (เดิมใช้ค่า desktop เดียวกันทุกจอ)

### 2. หน้าสอบสวน (commit `6ac3c03`)
- `.hud-left` (คอลัมน์ซ้าย 6 ปุ่ม) เลิกใช้ใน `openTrial()` แทนด้วย `.hud-menu` แถวนอนใหม่ —
  เป็นพี่น้องนอกกริดของ `.hud-body` (ไม่ใช่คอลัมน์) จึงไม่ต้องมี `grid-row` สลับลำดับมือถือแบบเดิม
- `.hud-menu` แบ่ง `.seg` (ask/st/cr/inten เลื่อนแนวนอนได้บนมือถือ) + `.go-cluster`
  (skip/jail/go ชิดขวาเสมอ) — `id`/`data-*` ทุกปุ่มไม่เปลี่ยนสักตัว logic เดิมคงหมด
- `dialog.hudwrap` 1180→1320px, `.port img` 52→96/72/60px (desktop/390/360) ขอบทอง 2px เรืองแสง
- กรอบทอง 2 ชั้น + `.corner-tick` 4 มุม + พื้นหลังไล่สีม่วง-ดำ ทั้ง `.hud`/`.hud-card`/`.port`
- พื้นผิวกระดาษ `img/tex-paper-hell.png` (คุณเป้ gen เอง ตามสเปก Mind) เป็น background ของ
  `.hud-rec` ผสม `multiply` กับ gradient ม่วงเดิม — เพิ่ม branch `tex-*` ใหม่ใน `prep-art.py`
  ให้ผ่าน pipeline แบบเดียวกับ `tile-*` (256×256 ไม่ครอป/ลอกพื้น) ทั้ง `prep()` และกัน
  `ingest()` เข้าใจผิดว่าเป็นต้นฉบับ

**ปัญหาที่เจอระหว่างทำและวิธีแก้ (ไม่ใช่แค่ตามตัวเลขสเปกตรง ๆ — สำคัญ อ่านก่อนรับงาน):**
- ตัวเลข `.hud-rec` ตามสเปก Vera ตรง ๆ (92px/76px มือถือ) **ไม่พอกับคดีสุ่มจริง** ที่มักมี 2-4
  บรรทัด (บุญ+กรรมที่รู้แล้ว) ไม่ใช่แค่ 1-2 บรรทัดสั้นที่สุดที่สเปกคำนวณไว้ — ทดสอบสุ่ม 25-50 คดี
  พบ overflow 24/25 ครั้งที่ตัวเลขสเปกตรง ๆ จึงขยับเป็น 175-195px จริง (ลดพื้นที่ `.hud-stage`
  ชดเชยแทน) เหลือ overflow 1/50 (2%) — ที่เหลือกันด้วย `overflow-y:auto` เป็นเกราะสำรอง ไม่ใช่
  `overflow:hidden` ที่จะตัดข้อความทิ้งเงียบ ๆ
- `.hud-top` เดิม chip กว้าง 42%+หลอด 70px ดันแถบสถานะกิน ~150px สูงบนจอ 360 (ไม่ใช่ที่ Vera
  ล็อกตัวเลขไว้ แต่ต้องแก้เพื่อให้ข้อ 3 ของคุณเป้เป็นจริง) — ย่อ chip/หลอดให้แน่นขึ้น เหลือ ~94px
- `.hud-stage` เตี้ยลงมากจนตัวละครสูงกว่ากรอบ ทะลุไปทับ `.hud-menu` ด้านบน (เจอจากสกรีนช็อตจริง
  ไม่ใช่เดา) — เพิ่ม `overflow:hidden` กันไว้
- `.hud-opt` เดิมปล่อย flex ไม่จำกัด ดันปุ่มออกหมายหลุดจอบนมือถือ (แม้ `#t-go` จะยังกดได้เพราะ
  อยู่ใน `.hud-menu` ที่ตรึงบนเสมอ) — คุมเพดาน 200px/118px ให้สกอลล์ภายในตัวเองแทนตามที่สเปก
  อนุญาตไว้แล้ว (หัวข้อ 2 ของสเปก)
- ยืนยันด้วย Playwright ว่า **`#t-go` อยู่ในจอเสมอ 20/20 ทุกขนาดจอ** (เกณฑ์เดียวที่สเปกบังคับ
  ตายตัวสำหรับปุ่มกลุ่มนี้) — แต่ `.hud-bottom` (การ์ดรูปยมน้อย/วิญญาณ+ไอเทม) อาจต้องเลื่อนกล่อง
  ทั้งใบเล็กน้อยเพื่อดูบนจอ 360×640 ถ้าคดีมีเนื้อหายาวพร้อมกันทั้ง `.hud-rec` และ `.hud-opt` —
  ไม่ผิดเกณฑ์รับงาน (ซึ่งระบุเฉพาะ `.hud-rec` และ `#t-go`) แต่เป็นข้อจำกัดจริงที่ควรรู้ไว้

### 3. Part H — ภาพโซน 3 (West) เพิ่มเติม + โซน 4 (CyberHell) ทั้งชุด
- **H1** (`43fa2e4`): ผ่าน `BG-dab-west.jpeg`/`BG-krata-west.jpeg` เข้า pipeline — สถานี
  "ป่าดาบอสิปัตตะ"/"กระทะทองแดง" ใช้ฉากห้องของตัวเองแล้ว (ก่อนหน้านี้ยังไม่มี ถอยไปใช้ฉากโซน 1)
- **H2** (`e44550a`): ผ่านภาพโซน 4 (CyberHell) ทั้งชุด 25 ไฟล์เข้า pipeline — BG- ห้องสถานี 5
  หลัง, อาคารสถานี 5 หลัง, ยมทูต 5 คน, บอส+โปรไฟล์, hero-yama+โปรไฟล์, นิรา+โปรไฟล์, ฉากมาถึงบอส,
  ฉาก intro เข้าโซน — **โซน 4 ยังไม่อยู่ใน `ZONES[]` จึงไม่เปิดให้เล่นจริงตามที่สั่ง** แค่เตรียม
  ไฟล์และยืนยัน path resolve ให้พร้อม
- **บั๊กจริงที่เจอและแก้ระหว่างทำ (เจอจากการเทียบรายชื่อไฟล์โซน 3 vs 4 ทีละคู่ ไม่ใช่เดา):**
  `img/raw/CyberHell/zone-boss-cyber.png` ชื่อดิบมีคำว่า "cyber" ติดมาด้วย ปกติ pipeline ต่อท้าย
  `-cyberhell` ให้เองจากชื่อโฟลเดอร์อยู่แล้ว ผลลัพธ์เลยกลายเป็น `zone-boss-cyber-cyberhell.png`
  (ซ้ำคำ) ขณะที่ `src/scene.js` เรียกคีย์ `'zone-boss'` เฉย ๆ — เกมหาไฟล์ไม่เจอ เงียบ ๆ ถอยไปใช้
  บอสโซน 1 แทน ไม่มี error ใด ๆ ให้เห็น แก้ด้วย `RENAME` dict ที่มีอยู่แล้วในสคริปต์ (จุดเดียวกับ
  ที่เคยแก้ "Boos Zone2" ของโซนบูรพา) **ไม่แตะไฟล์ raw**

## ไฟล์ที่แตะ
- `index.html` — CSS ทั้งหมด (token, `.corner-tick`, `.hud-btn`, `.arena*`, `.hud*`, media queries)
- `src/ui.js` — `arena()`, `openTrial()` (HTML template เท่านั้น ไม่แตะ logic)
- `scripts/prep-art.py` — branch `tex-*` ใหม่ (paper texture) + `RENAME` เพิ่ม `zone-boss-cyber`
- `img/tex-paper-hell.png` (ใหม่), `img/manifest.json`
- `img/West/BG-Dab-west.webp`, `BG-Krata-west.webp` (ใหม่)
- `img/CyberHell/*` — 15 ไฟล์ใหม่ + เปลี่ยนชื่อ 1 ไฟล์ (`zone-boss-cyber-cyberhell.png` →
  `zone-boss-cyberhell.png`)

## ไฟล์ที่ยังขาดในโซน 4 เทียบกับโซน 3 (ตามที่ Claudy ขอให้เช็ค)
เทียบรายชื่อไฟล์ทั้งโฟลเดอร์ `img/West/` กับ `img/CyberHell/` แบบ 1:1 (ตัดชื่อโซนออกก่อนเทียบ)
**ขาดแค่กลุ่มเดียว — ยืนยันตามที่ Claudy สงสัยถูกต้อง:**

| ไฟล์ที่ขาด (โซน 4) | มีในโซน 3 หรือไม่ |
|---|---|
| `crew-boon-cyberhell-profile.png` | ✅ มี (`crew-boon-west-profile.png`) |
| `crew-dam-cyberhell-profile.png` | ✅ มี |
| `crew-kan-cyberhell-profile.png` | ✅ มี |
| `crew-plerng-cyberhell-profile.png` | ✅ มี |
| `crew-taan-cyberhell-profile.png` | ✅ มี |

ไม่มีไฟล์อื่นขาดอีกแล้ว (เช็คครบทุกคู่: BG- ห้อง 5 หลัง, Boss+profile, Intro-Boss, intro-zone,
crew-nira+profile, hero-yama+profile, st-\* 5 หลัง, zone-boss — ตรงกันหมดยกเว้น 5 ไฟล์นี้)
ผลตอนนี้: ไม่มีโปรไฟล์ 5 ยมทูตของโซน 4 เกมจะถอยไปใช้ภาพ standee ปกติแทนในแผงข้อมูล (ไม่พัง
แค่ไม่มีรูปหน้าตรงในการ์ดโปรไฟล์) — รอคุณเป้ generate เพิ่มถ้าต้องการให้ครบ

## วิธีรัน/ทดสอบเอง
```bash
cd "/Users/agapae/Documents/Work PAE/Claude/AVEGEE"
python3 -m http.server 8791   # เปิด http://localhost:8791/index.html
```
ให้ Kittanate คลิกตรวจ:
1. เดินไปแท่นพิพากษา กด "🔍 เริ่มการสอบสวน" — ดูเมนูบาร์แนวนอนใหม่บนสุด กดครบทุกปุ่ม (ไต่สวน/
   ส่งไปที่ไหน/ใครคุม/หนักแค่ไหน/พักคดี/ขังไว้ก่อน/ออกหมาย) ว่าทำงานเหมือนเดิมทุกปุ่ม
2. ดูการ์ดสำนวน "สำนวนที่นิราอ่านให้ฟัง" ว่าอ่านจบไม่ต้องเลื่อน (ลองหลายคดีเพราะเนื้อหายาวไม่
   เท่ากัน) เห็นพื้นผิวกระดาษจาง ๆ ใต้ตัวอักษร
3. ดูการ์ดยมน้อย/วิญญาณมุมล่างว่าใหญ่ขึ้นชัดเจน มีกรอบทองเรืองแสง
4. ลองคดีที่ต้องสู้ (มีตัวขัดขืน) → เข้าฉากต่อสู้ ดูกรอบทอง+มุมฉลุ+ตัวละครใหญ่ขึ้น กดปุ่มต่อสู้
5. ทดสอบที่ 360×640 ก่อนขนาดอื่นเสมอ (ตึงที่สุด)
6. เข้าโซนปัจฉิม เปิดสถานี "ป่าดาบอสิปัตตะ"/"กระทะทองแดง" ดูฉากห้องใหม่

## สเปกอาร์ตที่ต้องการเพิ่ม
- โปรไฟล์ยมทูต 5 คนของโซน 4 (CyberHell): `crew-boon-cyberhell-profile.png`,
  `crew-dam-cyberhell-profile.png`, `crew-kan-cyberhell-profile.png`,
  `crew-plerng-cyberhell-profile.png`, `crew-taan-cyberhell-profile.png` — สเปกเดียวกับ
  `crew-*-west-profile.png` ที่มีอยู่แล้ว (jpeg ก็ได้ ไม่ต้องพื้นใส ครอปจัตุรัสอัตโนมัติ)

## สิ่งที่ยังไม่ทำ
- `.orn` ครีบตกแต่งกึ่งกลางด้านบนกรอบ — ระบุในสเปกว่า "ทำได้ถ้ามีเวลา" ไม่ใช่ของบังคับ ข้ามไปก่อน
- ยังไม่ตรวจสอบว่า `.hud-bottom` ต้องเลื่อนกล่องทั้งใบเพื่อดูบนจอ 360×640 ในบางคดี (ไม่ผิดเกณฑ์
  รับงานตามที่สเปกระบุไว้ตรง ๆ แต่เป็นข้อจำกัดจริงที่ Chris/คุณเป้ควรรู้ก่อนตรวจรับ)
- โซน 4 ยังไม่เปิดให้เล่น (ตามที่สั่งชัดเจนว่ายังไม่ใส่ใน `ZONES[]`)
- โปรไฟล์ยมทูตโซน 4 5 คน (ดูหัวข้อสเปกอาร์ตด้านบน)
