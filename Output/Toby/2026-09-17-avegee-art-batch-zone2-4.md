# อเวจี — ภาพชุดใหม่โซน 2-4 + ปิดชุดแก้ 17 ก.ย. + ใส่ข้อความที่ผ่านตรวจแล้ว

**ใบงาน:** `Output/Claudy/briefs/2026-09-17-avegee-art-batch-zone2-4.md`
**repo:** `/Users/agapae/Documents/Work PAE/Claude/AVEGEE` · โหมด claude-only (ไม่ได้เรียก Codex)
**สถานะ:** ทำครบทั้ง 3 ส่วน (A/B/C) · commit 7 ครั้ง push ขึ้น `origin/main` แล้ว · `git status` สะอาด

## สิ่งที่ build

### ส่วน A — ปิดชุดแก้ `2026-09-17-avegee-fix-batch.md`
1–2. **บทลงโทษพ่อ 3 ครั้ง + เพลงฉากสู้** — เช็คแล้วพบว่า **ทำไปแล้วก่อนหน้าเซสชันนี้** (commit `91c962a`,
   `fdf7283` มีอยู่ในประวัติแล้ว และ CONCEPT.md §22.6 อัปเดตตรงกับโค้ดแล้ว) — ไม่ต้องทำซ้ำตามกติกาใบงาน
3. **`.gitignore` + เลิกแทร็ก `img/raw/`** — เพิ่ม `img/raw/` และ `agent-pilot/` ใน `.gitignore` แล้ว
   `git rm --cached -r img/raw` เลิกแทร็กไฟล์เดิมที่หลุดเข้า repo ไปแล้ว 155 ไฟล์ (**ไฟล์ยังอยู่บนดิสก์ครบ
   ไม่ได้ลบ** — เช็คแล้วว่า `img/raw/spirit4.png` ฯลฯ ยังอยู่จริงหลัง commit)
5. **รูปวิญญาณผิดเพศ (A4)** — หาต้นเหตุจริงแล้วพบว่า `SPIRIT_SEX[4]` ถูกจัดเป็น `'n'` (ดูไม่ออก) ทั้งที่
   เปิดไฟล์ `img/spirit4.png` จริงแล้วอ่านเป็นหญิงชัดเจน (ผมยาวสยาย หน้าคมบาง) — แก้เป็น `'f'`
   และพบว่า `mkCaseSoul()` (สำนวนเขียนมือ) ไม่เคยเช็คเพศเลย ใช้ `c.sp` ตรง ๆ ต่างจากวิญญาณสุ่มที่มี
   `spiritFor()` กันอยู่แล้ว — แยก logic เป็น `safeSp(sp, sex)` ใช้ร่วมกันทั้งสองทาง กันบั๊กแบบนี้ซ้ำในอนาคต
   ไล่ตรวจครบทุกสำนวนเขียนมือ (TH 11 + Asia 20) เทียบรูปจริงกับ `sex` — เจอปัญหาเฉพาะ A4 ตัวเดียว
   (ดูตารางเต็มด้านล่าง)
6. **โซน 2 ไม่มีไอเท็มให้เก็บ** — **ทดสอบแล้วไม่พบบั๊ก** ทั้งไอเท็มทั่วไปบนแผนที่ (`dropItem`/`ITEM_SPOTS`)
   และไอเท็มประจำสถานี (`stationDrops`/`ROOMS[k].item`) ทำงานเหมือนกันทุกประการระหว่างโซนไทยกับโซนบูรพา
   — ยืนยันด้วยการรันจริงในเบราว์เซอร์ (ไม่ใช่แค่อ่านโค้ด): สร้างศาลาน้ำชาในโซนบูรพา เปิดหน้าสถานีจริง
   เห็น "เดินไปเก็บถ้วยน้ำชาร้อน" ขึ้นถูกต้อง (ภาพชัดในรายงานนี้ไม่ได้แนบ แต่ทดสอบผ่าน screenshot จริงแล้ว)
   สถานีลงทัณฑ์ (`sala/krata/dab/lokan/ngiw`) ไม่เคยมีไอเท็มในทั้งสองโซนอยู่แล้ว — เป็นดีไซน์เดิม ไม่ใช่บั๊ก
   ตั้งสมมติฐานว่าอาจเป็นรายงานเก่าที่แก้ไปแล้วในงานก่อนหน้า หรือ Kittanate เจอตอนยังไม่ได้สร้างสถานีที่มี
   `visit.drop` เลยในโซนนั้น — **ไม่มีโค้ดให้แก้ ไม่มี commit สำหรับข้อนี้**

### ส่วน B — ต่อสายภาพชุดใหม่โซน 2-4
- รัน `scripts/prep-art.py` แปลง `img/raw/<Zone>/` → `img/<Zone>/` ตามงบ §18.3 (BG-* = webp q82,
  ที่เหลือ png 512×512) แล้วอัปเดต `img/manifest.json` — **ไม่ต้องแก้โค้ดสักบรรทัดเดียว** เพราะระบบ
  `artUrl()`/`stationBox()` ที่มีอยู่แล้วหาไฟล์ตามชื่อคีย์ให้เองทันทีที่ผ่าน manifest
- **ไอคอนสถานีโซน 3** (`st-dab/krata/lokan/sala-west`) — ไฟล์เดิมขึ้นต้นด้วย `_` (แปลว่า "ยังไม่เอาเข้า"
  ตาม CONCEPT.md เดิม) ตัด `_` ออกตามที่ใบงานนี้สั่งชัดเจนแล้วรันสคริปต์ — ยืนยันด้วย `img()` โหลดไฟล์โซน
  west สำเร็จจริงทั้ง 4 หลัง (`got: true`) และเห็นอาคารจริงในสกรีนช็อต ไม่ใช่กล่องเปล่า
  ชื่อไฟล์ตรงกับ `STATIONS[].k` ตรงตัวอยู่แล้ว (dab/krata/lokan/sala) — คำอธิบาย "โลกัน/งิ้ว=ต้นงิ้ว" ใน
  ใบงานคลาดเคลื่อน (lokan คือ "โลกันตนรก" ไม่ใช่ต้นงิ้ว/ngiw — ยึดตามโค้ดจริงตามที่ใบงานสั่งไว้)
- **ชุดแต่งกาย hero-yama-cyberhell / crew-nira-cyberhell** — ไฟล์ประมวลผลแล้วพร้อมใช้ ต่อสายเข้าระบบ
  ชุดเดิมอัตโนมัติผ่าน `heroStyleOf()`/`zoneOf()` (ไม่มีโค้ดใหม่) — แต่ยังเลือกใช้จริงไม่ได้จนกว่าโซน 4
  จะเปิดเล่น (ไม่มีใน `outfitsOpen()` เพราะ `ZONES` ไม่มี key `cyberhell`)
- **ยังไม่ได้ต่อสาย:** `intro-zone2-asia.png` และ `intro-zone3-west.png` — ไฟล์ประมวลผลแล้ว อยู่ใน
  `img/Asia/` และ `img/West/` พร้อมใช้ แต่ยังไม่มีจุดในโค้ดที่ต้องการภาพประกอบตอน "ย้ายเข้าโซนใหม่"
  (จุดนั้นตอนนี้ใช้ `bossModal()` แบบข้อความล้วน ไม่มีรูป) — **ไม่ได้แตะเพราะไม่ชัดว่าใบงานต้องการให้
  เปลี่ยน UI ของกล่องต้อนรับเข้าโซนด้วยหรือแค่เตรียมไฟล์ไว้ก่อน** ถามคุณเป้/Vera ก่อนถ้าต้องการให้ใส่

### ส่วน C — ใส่ข้อความที่ผ่านตรวจแล้ว
**C-A บอสผู้ตรวจการ** — เพิ่มฉากมาถึง 3-4 บรรทัด (สลับผู้พูด) ก่อนสู้บอสครั้งแรกของทุกโซนที่เล่นได้
(ใช้ `Intro-Boss-Zone<N>` เป็นพื้นหลังที่ต่อสายในส่วน B) กดผ่านทีละบรรทัดแบบเดียวกับบทนำเกม (คอมโพเนนต์
`.intro-comic` เดิม ไม่สร้าง UI ใหม่) รีแมตช์ข้ามฉากอัตโนมัติ (`bossArriveSeen`) บทตอนบอสแพ้/ยมน้อยแพ้
ใช้ข้อความของ Rae แทน log ทั่วไป โซน 4 เก็บบทไว้ที่ `ZONE4_BOSS_DRAFT` แยกจาก `ZONES[]` ตามที่สั่ง
(ยังเปิดเล่นไม่ได้ ไม่ได้สร้างเนื้อหาใหม่เอง)

**C-B ประเด็นสอบสวน A1-A20** — แทนที่แม่แบบเดียวกันหมดด้วยบทเฉพาะรายสำนวนของ Rae (ผ่าน Reese
fact-check รอบ 3 ครบ) เพิ่มพารามิเตอร์ `claims` ให้ `C()` รับได้ (ไม่ส่งมา = ถอยไปแม่แบบเดิม)

**C-C ข้อความท้ายหน้าปก** — เพิ่ม "สำนวนในเกมเป็นเรื่องแต่งขึ้นเพื่อความบันเทิงเท่านั้น" ไว้ล่างสุดของ
หน้าปก ตัวเล็กกว่าบรรทัดเดิม คำว่า "ยมบาท" ไม่แตะตามที่คุณเป้ตัดสิน

## ⚠️ สิ่งที่พบระหว่างทดสอบ ต้องรายงานคุณเป้/Claudy (ไม่ใช่ของที่ฉันมีสิทธิ์ตัดสินเอง)

1. **เนื้อหาสำนวน A7/A11/A16/A18/A20 ไม่ตรงกันระหว่าง `hidden[]` (แฟ้มคดี) กับ `claims[]` (บทสอบสวน)**
   — ใบงาน Rae ขอบเขตแค่แทนที่ `claims[]` เท่านั้น (read-only ต่อ `seen`/`hidden`/`line`) แต่ข้อความ
   defingerprint ใหม่ใน `claims[].reveal` พูดถึงกลไกที่ต่างจาก `hidden[0].t` เดิมในไฟล์ (ยังเป็นเวอร์ชัน
   ก่อน defingerprint เช่น A7 `hidden` ยังเขียนว่า "สั่งปล่อยสารพิษ..." แต่ `claims` ใหม่พูดว่า "หนีพร้อม
   ทรัพย์...") ถ้าผู้เล่นขุดจนเห็น `hidden` ในแฟ้มจะเจอข้อความคนละเรื่องกับที่ปากพูด — ควรส่ง Rae แก้
   `seen`/`hidden` ให้ตรงกับกลไกใหม่ด้วยอีกรอบ (นอกขอบเขตงานนี้ ไม่ได้แตะเอง)
2. **`.foot` เดิมของหน้าปกล้นจอที่ 360×640 อยู่ก่อนแล้ว** (ไม่ใช่จากบรรทัดที่เพิ่มวันนี้) — `.menu` ไม่มี
   `overflow-y:auto` นอก breakpoint `max-height:520px` วัดจริง: `.foot` เดิม bottom = 656px ที่วิวพอร์ต
   สูง 640px (ล้นอยู่ก่อนแล้ว 16px) ข้อความใหม่ที่เพิ่มดันล้นเพิ่มอีกนิด — ไม่ได้แก้เพราะเป็น layout bug
   เดิมที่กว้างกว่าใบงานนี้ (ส่งต่อให้ Vera/Claudy ตัดสินว่าจะขยาย breakpoint หรือทำอย่างไร)
3. **`img/West/Boss Zone3-west.png` / `img/Asia/Boos Zone2-asia.png` / `img/CyberHell/Boss Zone4-cyberhell.png`
   ยังไม่ได้ต่อสาย** — เดาว่าน่าจะเป็นภาพ "หน้าอกบอส" (portrait) สำหรับใช้ในกล่องฉากมาถึง คู่กับ
   `Intro-Boss-Zone<N>` (พื้นหลัง) คล้ายที่ `hero-boss` ใช้ในบทนำเกม แต่**ไม่กล้าเดาแล้วใส่เอง** เพราะ
   ใบงานไม่ได้ระบุตำแหน่งใช้งานชัดเจน และคีย์ `zone-boss-<zone>` (ตัวบอสในฉากต่อสู้จริง) มีไฟล์แยกอยู่แล้ว
   ทำงานถูกต้อง — ถามคุณเป้ว่าไฟล์ชุดนี้มีไว้ทำอะไรก่อนต่อสาย (ชื่อไฟล์ยังมีคำผิด "Boos" ในโซน 2 ด้วย
   — น่าจะเป็นพิมพ์ผิดจากต้นฉบับ แจ้งไว้เฉย ๆ ไม่ได้แก้ชื่อไฟล์เอง)

## ตารางไฟล์ภาพ (ก่อน/หลังงบ §18.3)

| ไฟล์ | ก่อนแปลง | หลังแปลง | ใช้ที่ไหน |
|---|---|---|---|
| `img/Intro-Boss-Zone1.png` | jpeg ~ไม่ทราบ (ต้นฉบับ) | png 512×512 | พื้นหลังฉากมาถึงบอสโซน 1 |
| `img/Asia/Intro-Boss-Zone2-asia.png` | jpeg | png 512×512 | พื้นหลังฉากมาถึงบอสโซน 2 |
| `img/Asia/intro-zone2-asia.png` | png | png 512×512 | ยังไม่ได้ต่อสาย (ดูหัวข้อด้านบน) |
| `img/Asia/BG-Lan-asia.webp` | jpeg | webp q82 | พื้นหลังลานโซนบูรพา (ผ่าน `stBg()` อัตโนมัติ) |
| `img/West/intro-zone3-west.png` | jpeg | png 512×512 | ยังไม่ได้ต่อสาย |
| `img/West/Intro-Boss-Zone3-west.png` | png | png 512×512 | พื้นหลังฉากมาถึงบอสโซน 3 |
| `img/West/BG-Lan-west.webp` | jpeg | webp q82 | พื้นหลังลานโซนปัจฉิม |
| `img/West/st-dab-west.png` | png (ขึ้นต้น `_`) | png 512×512 | อาคารสถานีป่าดาบอสิปัตตะ โซน 3 |
| `img/West/st-krata-west.png` | png (ขึ้นต้น `_`) | png 512×512 | อาคารสถานีกระทะทองแดง โซน 3 |
| `img/West/st-lokan-west.png` | png (ขึ้นต้น `_`) | png 512×512 | อาคารสถานีโลกันตนรก โซน 3 |
| `img/West/st-sala-west.png` | png (ขึ้นต้น `_`) | png 512×512 | อาคารหอทะเบียนกรรม โซน 3 |
| `img/West/hero-yama-west.png` `-profile` | png/jpeg | png 512×512 | ชุดยมน้อยธีมโซน 3 (ผ่านปุ่มเปลี่ยนชุด) |
| `img/West/crew-nira-west.png` `-profile` | png/jpeg | png 512×512 | ชุดนิราธีมโซน 3 |
| `img/CyberHell/Intro-Boss-Zone4-cyberhell.png` | png | png 512×512 | พื้นหลังฉากมาถึงบอสโซน 4 (เก็บไว้ ยังเล่นไม่ได้) |
| `img/CyberHell/BG-Lan-cyberhell.webp` | jpeg | webp q82 | พื้นหลังลานโซน 4 (เก็บไว้) |
| `img/CyberHell/hero-yama-cyberhell.png` `crew-nira-cyberhell.png` | png | png 512×512 | ชุดโซน 4 (เก็บไว้ ยังเลือกใช้ไม่ได้จนเปิดโซน) |
| งบรวม manifest | asia 8.2 MB / west 1.7 MB / cyberhell 1.1 MB (ก่อนรอบนี้) | asia 8.7 MB / west 2.5 MB / cyberhell 1.1 MB | อยู่ในงบ §18.3 (ผู้เล่นโหลดเฉพาะชุดโซนที่ไปถึง) |

## ตารางตรวจรูปวิญญาณ vs เพศ (สำนวนเขียนมือทั้งหมด)

| โซน | สำนวน | sex | รูปที่ใช้ | ผล |
|---|---|---|---|---|
| TH | pol/gen/recruit/star/lord/deva | m | soul-* เฉพาะตัว | ✅ ตรง |
| TH | nun/nurse | f | soul-* เฉพาะตัว | ✅ ตรง |
| TH | girl/boy/monk | g/b/monk | soul-* เฉพาะตัว | ✅ ตรง |
| Asia | A1-A7,A9,A10,A13,A14,A15,A17-A20 | m | sp 1/2/3/5/8 | ✅ ตรง (ทุกตัวเป็น 'm'/'n' ในตาราง) |
| Asia | **A4** | m | sp 4 (**เคยเป็นหญิง**) | ✅ **แก้แล้ว** — `safeSp` สลับเป็น sp 2 อัตโนมัติ |
| Asia | A8,A12 | f | sp 6 | ✅ ตรง |
| Asia | A11 | f | sp 9 | ✅ ตรง |
| Asia | A16 | f | sp 7 | ✅ ตรง |

## ไฟล์ที่แตะ

**โค้ด**
- `src/data.js` — `safeSp()`, `SPIRIT_SEX[4]` แก้, `ZONES[].bossSub/bossTalk/bossArrive/bossWin/bossLose`, `ZONE4_BOSS_DRAFT`
- `src/game.js` — `mkCaseSoul()` ใช้ `safeSp`, `bossArriveSeen` (init/snapshot/restore), `endBattle()` ใช้ `bossWin/bossLose`
- `src/ui.js` — `openBossArrive()`, จุดเรียกในช่วง `g.onChange` ก่อนเข้าสู้บอส
- `src/cases-asia.js` — `C()` รับ `claims` ได้, `K()` helper, แทนที่ claims ทั้ง 20 สำนวน
- `index.html` — เพิ่ม `.disclaimer` (ข้อความท้ายหน้าปก) + CSS
- `.gitignore` — เพิ่ม `img/raw/`, `agent-pilot/`
- `CONCEPT.md` — อัปเดต §22.3 (ไอคอนสถานีโซน 3 เอาเข้าแล้ว), §22.5 (ฉากมาถึงบอส)

**อาร์ต** (ทั้งหมดผ่าน `scripts/prep-art.py`) — ดูตารางด้านบน · เลิกแทร็ก `img/raw/*` (155 ไฟล์ ยังอยู่บนดิสก์)

**Commit hash (เรียงตามลำดับ):**
1. `9cc19f7` — `.gitignore` + เลิกแทร็ก `img/raw/`
2. `687b1db` — แก้รูปวิญญาณผิดเพศ A4 (`safeSp`)
3. `d3692b6` — ต่อสายภาพชุดใหม่โซน 2-4
4. `203d80d` — ฉากมาถึงบอส + บทแพ้/ชนะ
5. `4932239` — ประเด็นสอบสวน A1-A20
6. `83b7799` — ข้อความท้ายหน้าปก
7. `a126c6e` — อัปเดต CONCEPT.md

push ขึ้น `origin/main` สำเร็จแล้ว (`fdf7283..a126c6e`)

## วิธีรัน/ทดสอบเอง

```bash
cd "/Users/agapae/Documents/Work PAE/Claude/AVEGEE"
python3 -m http.server 8778
# เปิด http://localhost:8778/index.html
```

Kittanate ลองกดตามนี้ (ทดสอบจริงในเบราว์เซอร์ทุกข้อ ไม่ใช่แค่อ่านโค้ด):
1. เริ่มเกมใหม่ → เล่นโซนไทยจนตัดสินครบ 10 สำนวน → **ต้องเห็นฉากมาถึง "ยมราชพี่ใหญ่" 4 บรรทัดก่อนสู้**
   (พื้นหลังภาพใหม่ ไม่ใช่จอเปล่า) กดจนจบแล้วเดินข้ามสะพานเข้าสู้จริง
2. ท้าสู้บอสซ้ำ (แพ้แล้วลองใหม่) — **ต้องไม่เห็นฉากมาถึงซ้ำ** เข้าสู้ตรง ๆ เลย
3. ย้ายไปโซนบูรพา (ต้อง Lv3 + ผ่านบอสโซนไทย) → สร้างศาลาน้ำชา (120 เบี้ยกรรม) → เปิดหน้าสถานี
   ต้องเห็น "เดินไปเก็บถ้วยน้ำชาร้อน" ในฉาก (ยืนยันว่าไอเท็มโซน 2 มีจริง)
4. เล่นสำนวน A-ต่าง ๆ ในโซนบูรพา กดปุ่ม "ไต่สวน" — ข้อความแต่ละสำนวนต้องไม่ซ้ำกัน (ก่อนหน้านี้ทุก
   สำนวนพูดเหมือนกันหมด)
5. ย้ายไปโซนปัจฉิม (west) → สร้างสถานี "ป่าดาบอสิปัตตะ/กระทะทองแดง/โลกันตนรก/หอทะเบียนกรรม" —
   **ต้องเห็นอาคารมีรูปจริงของโซน ไม่ใช่กล่องเทาเปล่า**
6. เปิดหน้าปกเกม (ออกจากเกมกลับเมนู) — **ต้องเห็นข้อความเล็ก ๆ ล่างสุดใต้ทุกอย่าง**:
   "สำนวนในเกมเป็นเรื่องแต่งขึ้นเพื่อความบันเทิงเท่านั้น"
7. เปิด DevTools Console ตลอดการเล่น — ไม่ควรมี error สีแดงใหม่ (404 ของ `bgm-battle`/`hero-yama-side`
   เป็นของเดิมที่รู้อยู่แล้ว ไม่ใช่ของใหม่จากงานนี้)

**เครื่องมือที่ใช้ทดสอบจริง (ไม่ใช่แค่คาดเดา):** Playwright headless Chromium ที่ 390×844 และ 900×900
เขียนสคริปต์ไดรฟ์เกมจริงผ่าน `window.G` (เกมเปิดให้ debug ไว้อยู่แล้ว) คลิกจริง อ่าน state จริง
ถ่าย screenshot จริงเทียบผลก่อน/หลัง ไม่ได้อนุมานจากการอ่านโค้ดอย่างเดียว

## สเปกอาร์ตที่ต้องการเพิ่ม

ไม่มีสเปกใหม่ที่ต้องวาดเพิ่มสำหรับรอบนี้ — ของที่ส่งมาครบใช้งานได้เกือบหมด เหลือแค่ 3 ไฟล์ที่ไม่รู้
วัตถุประสงค์แน่ชัด (`Boss Zone<N>` portrait) ตามที่แจ้งในหัวข้อ "สิ่งที่พบ" ด้านบน — ไม่ใช่ของที่ขาด
วาด แต่เป็นของที่ส่งมาแล้วไม่รู้จะเอาไปแปะตรงไหน ขอคุณเป้ยืนยันก่อน

**ไอคอนสถานีที่ยังขาดจริง** (นอกขอบเขตงานนี้ตามที่ Claudy สั่งไว้ชัดว่า "ไม่ต้องทำ"):
- CyberHell: `_st-*-cyberhell` (4 อัน) + รูปโปรไฟล์ยมน้อย/นิราธีมโซน 4
- โซนบูรพา: ไอคอนสถานีธีมโซนตัวเอง (ตอนนี้ยังใช้รูปโซน 1 แทนทุกหลัง)

## สิ่งที่ยังไม่ทำ

1. **`intro-zone2-asia.png` / `intro-zone3-west.png`** — ไฟล์พร้อมใช้แล้วแต่ไม่ได้ต่อสาย เพราะไม่ชัดว่า
   ต้องเปลี่ยน UI กล่อง "ย้ายเข้าโซนใหม่" (ตอนนี้เป็นข้อความล้วน) ให้มีรูปด้วยหรือเปล่า — รอคุณเป้/Vera ตัดสิน
2. **`Boss Zone1/2/3/4` portrait** — ไม่ได้ต่อสายเพราะไม่รู้จุดใช้งานที่แน่ชัด (ดูหัวข้อ "สิ่งที่พบ" ข้อ 3)
3. **CyberHell / โซนบูรพา ไอคอนสถานี** — ไม่ทำตามคำสั่ง Claudy ที่บอกว่านอกขอบเขตรอบนี้
4. **`hidden[]`/`seen[]` ของ A7/A11/A16/A18/A20 ยังไม่ตรงกับ `claims[]` ใหม่** — ต้องส่ง Rae แก้แฟ้มคดี
   ให้ตรงกับกลไก defingerprint อีกรอบ (นอกขอบเขต read-only ของใบงาน Rae เดิม)
5. **`.foot` ล้นจอที่ 360×640** — บั๊กเดิมก่อนงานนี้ ไม่ได้แก้ ส่งต่อให้ตัดสินใจว่าใครควรแก้
6. **โซน 4 (นรกเครือข่าย) ยังเปิดเล่นไม่ได้** — ตามที่ตั้งใจ ไม่ได้สร้างเนื้อหาใหม่เอง เก็บ `ZONE4_BOSS_DRAFT`
   ไว้ล่วงหน้าเท่านั้น
