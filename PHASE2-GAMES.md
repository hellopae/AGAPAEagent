# เฟส 2 — MiniGame ในออฟฟิศ (แผนงาน)

> เขียนไว้คืน 25 ส.ค. 2569 ตอนใกล้ชนลิมิต — เริ่มลงมือได้เลยจากเอกสารนี้
> ไม่ต้องไปไล่อ่านโค้ดซ้ำ ทุกอย่างที่ต้องรู้อยู่ในนี้แล้ว
>
> **เฟส 1 (EXP / LV / ระดับ / MVP) ทำเสร็จและ push แล้ว** ทั้งเว็บและ Widget
> โค้ดอยู่ที่หัวข้อ `LEVEL` ใน `index.html` และ `Claude/Widget/src/levels.js`

---

## สรุปสิ่งที่ตัดสินใจไปแล้ว (ไม่ต้องถามซ้ำ)

| เรื่อง | ข้อสรุป |
|---|---|
| ขอบเขต | ทำทั้งหมด 4 รอบ จบแต่ละรอบ push + ขึ้น Widget |
| Board แชมป์ | **แชมป์ = Agent ถือครอง** — agent แต่ละคนมีสถิติของตัวเอง เป้เล่นชนะเมื่อไหร่ก็แย่งแชมป์มา แล้ว agent พูดยอมรับ/ขอแก้มือ |
| เก็บสถิติที่ไหน | Firestore `agents/games` — **เว็บเขียนเองได้** ยืนยันแล้ว ดูตัวอย่างที่ `vocab.html:443` และ `:584` (`DOC.set({...}, {merge:true})`) ไม่ต้องผ่านสคริปต์บนเครื่อง |
| ไฟล์เกม | แยกเป็น `games.js` **ห้ามยัดเพิ่มใน index.html** (ตอนนี้ 197k / ~3,470 บรรทัดแล้ว) โหลดด้วย `<script src="games.js">` — GitHub Pages เสิร์ฟตรง ไม่มี build step |
| ฝั่ง Widget | แสดง**แค่ board แชมป์** (อ่าน `agents/games`) + ปุ่มเปิดเว็บไปเล่น — **ไม่พอร์ตเกมไปเขียนใหม่เป็น React** เพราะจอ 600×640 เล่น Sudoku/หมากรุกอึดอัด และจะต้องดูแลโค้ดเกมสองชุด |

---

## เกมไหน วางตรงไหน แข่งกับใคร

| เกม | จุดในออฟฟิศ | คู่แข่ง | เหตุผลที่เลือกคนนี้ |
|---|---|---|---|
| 🏓 ปิงปอง | โต๊ะปิงปอง ห้องเกม | **Toby** | เจ้าถิ่นห้องเกม เป็นแชมป์ตั้งต้น |
| ⚽ เตะบอลเข้าโกล | ทางเดินกลางล่าง (จุดที่ลูกบอลอยู่) | **Dale** เป็นโกล | บทพูดเดิมของเขาคือ "อย่าให้กลิ้งเข้าห้องแร็ค" |
| 🎣 ตกปลา | ตู้ปลา ห้องพัก | เล่นคนเดียว | น้องส้มนั่งลุ้นข้างๆ (มี `OFC_FISH_POS` อยู่แล้ว) |
| 🐍 เกมงู | PS5 หน้าทีวี ห้องเกม | เล่นคนเดียว ทำสกอร์ | จอทีวีมีเอฟเฟกต์แสงตอนมีคนเล่นอยู่แล้ว |
| 🧱 Tetris | PS5 เหมือนกัน (เมนูเลือกเกมบนจอ) | เล่นคนเดียว | |
| 🔢 Sudoku | ห้องสเปค | **Vera** แข่งจับเวลา | บุคลิกเธอ "เน้นข้อเท็จจริง วัดได้ ทุกสเปคต้องเป๊ะ" |
| ♟️ หมากรุก | ห้องประชุม (ยังไม่มีใครนั่ง) | **Claudy** | "สุขุม วิเคราะห์เก่ง รู้เสมอว่าสเต็ปต่อไปคืออะไร" |
| 🕹️ RushExit | ตู้เกมในห้องพัก | — | ลิงก์ออกไป https://hellopae.github.io/RushExit/ |

### Interactive เพิ่ม (ไม่ใช่เกม แต่เพิ่มชีวิตให้ออฟฟิศ)
- **เครื่องชงกาแฟแพนทรี่** — กดชงให้ agent ที่เดินมา เขาเด้งขอบคุณตามบุคลิก
- **กระดานงานค้างศูนย์คุมงาน** — กดแล้วโยนงานให้คนที่ว่าง (เชื่อมกับ `todo.json`)

---

## ของที่ "มีอยู่แล้ว" ในออฟฟิศ — ต่อปุ่มเข้ากับพวกนี้ได้เลย

ทั้งหมดอยู่ในหัวข้อ `OFFICE` ของ `index.html` ระบบพิกัดอิงรูป `office/floor2.2.png` ขนาด **1586 × 992**

```
OFC_PP_ART / OFC_PP_POS [430,800] / OFC_PP_SIDE {A:[300,812], B:[560,812]}   โต๊ะปิงปอง (ยืนได้ 2 ข้าง)
OFC_PS5_ART / OFC_PS5_POS [182,884] / OFC_PS5_SIDE {A:[118,862], B:[180,862]} PS5
OFC_TV_POS [128,904]                                                          จอทีวี (มีเอฟเฟกต์แสง)
OFC_TANK_ART / OFC_TANK_POS [1180,810] / OFC_FISH_STAND [1290,878]            ตู้ปลา + จุดยืนดูปลา
OFC_BALL / OFC_BALL_HOME [790,645] / OFC_BALL_NODE 'B5'                       ลูกบอลบนทางเดิน
OFC_BOARD {x:548, y:368, w:176}                                               กระดานงานค้าง
```

ห้องที่เกี่ยวข้อง (จาก `OFC_ROOMS`):
- `bm` ห้องเกม — Toby นั่งอยู่ · `meet` ห้องประชุม — **ว่าง ใช้วางหมากรุกได้**
- `r5` ห้องสเปค — Vera · `lounge` ห้องพัก — ตู้ปลา + ที่นอนแมว · `pantry` แพนทรี่ — ว่าง

ฟังก์ชันที่จะต้องใช้ต่อ:
- `ofcNextGoal(ac)` — ตัวเลือกกิจกรรมของ agent มี `pp` / `ps5` / `fish` / `ball` อยู่แล้ว ตอนเปิดเกมต้องกันไม่ให้ agent คู่แข่งเดินหนี
- `ofcSay(ac, act)` / `ofcCatSay()` — ให้ตัวละครพูดตอนแพ้/ชนะ
- `ofcBuildProps()` — ที่วางของในผัง เพิ่มปุ่มกดตรงนี้

---

## ลำดับการทำ

**รอบ 1 — วางฐาน + 3 เกมที่ใช้ของเดิม**
1. `games.js` — game shell (modal เปิด/ปิด, กันคีย์ลัดชนกับหน้าเว็บ, ปิดแล้วคืนโฟกัส)
2. Board แชมป์ — โครง `agents/games` + ฟังก์ชันอ่าน/เขียน + การ์ดกระดานแชมป์บนหน้าเว็บ
3. 🏓 ปิงปอง vs Toby (กดจังหวะรับ-ส่ง) · ⚽ เตะบอล vs Dale (เล็งมุม+คุมแรง) · 🎣 ตกปลา (จับจังหวะ)
4. ต่อปุ่มเข้ากับโต๊ะปิงปอง / ลูกบอล / ตู้ปลา ในผัง

**รอบ 2** — 🐍 งู + 🧱 Tetris (PS5, มีเมนูเลือกบนจอทีวี) · 🔢 Sudoku vs Vera

**รอบ 3** — ♟️ หมากรุก vs Claudy
> ของเดิมที่ `Claude/Chess/chess.html` (556 บรรทัด) เป็น **2 คนเล่น ไม่มี AI** — กติกาการเดินหมากเอามาใช้ต่อได้ แต่ต้องเขียน AI ใหม่ทั้งหมด: minimax + alpha-beta ลึก 2–3 ชั้น, ตารางค่าตำแหน่งหมาก, 3 ระดับความยาก, ให้ Claudy พูดคอมเมนต์ตอนเดิน

**รอบ 4** — ☕ เครื่องชงกาแฟแพนทรี่ · 🕹️ ตู้เกม RushExit · 📋 กระดานโยนงาน

จบทุกรอบ: อัปเดต `worklog.json` + `status.json` → `git add -A && git commit && git push origin main` (ตามกฎเดิม ไม่ต้องถาม)

---

## โครงข้อมูล `agents/games` (ร่าง)

```js
{
  updatedAt: "2026-08-26T...Z",
  games: {
    pingpong: {
      label: "ปิงปอง", emoji: "🏓", room: "bm",
      champion: "toby",          // agent id หรือ "pae" ถ้าเป้ถือแชมป์อยู่
      streak: 12,                // แชมป์คนปัจจุบันป้องกันได้กี่ครั้ง
      best: { who: "toby", score: 248 },
      vsPae: { win: 8, lose: 15 },   // สถิติเป้ ชนะ/แพ้
      lastPlayed: "2026-08-26T...Z"
    },
    football: { ... }, fishing: { ... }, snake: { ... },
    tetris: { ... }, sudoku: { ... }, chess: { ... }
  }
}
```

กติกาแชมป์: เป้ชนะ agent ที่ถือแชมป์ → `champion` เปลี่ยนเป็น `"pae"` และ `streak` รีเซ็ตเป็น 1
เกมเล่นคนเดียว (งู/Tetris/ตกปลา) → ใครสกอร์สูงสุดถือแชมป์ agent ทำสกอร์เองได้ตอนที่เดินไปเล่นในผัง

---

## กับดักที่เจอมาแล้ว — อย่าเหยียบซ้ำ

- **ท่าเดินของคนปิดอยู่** (`OFC_WALK = {}`) เพราะสไปรท์ชีตแต่ละคนสเกล/ทิศทางไม่ตรงกัน เดินถอยหลังบ้าง ตัวใหญ่ไม่เท่ากันบ้าง — **อย่าเปิดกลับโดยไม่ทำชีตให้เท่ากันก่อน** ไฟล์ยังอยู่ที่ `office/sprites/*-W.png`
- **สูตรเลเวลมี 2 ที่ ต้องตรงกัน** — `index.html` หัวข้อ `LEVEL` กับ `Claude/Widget/src/levels.js` แก้ที่เดียวแล้วลืมอีกที่ เลเวลบนเว็บกับ Widget จะไม่เท่ากัน
- **`agents/email` และ doc อื่นใน `agents/` อ่านได้ด้วย API key ที่ฝังในหน้าเว็บสาธารณะ** — `agents/games` ก็เหมือนกัน อย่าเก็บอะไรที่เป็นความลับลงไป (สถิติเกมไม่เป็นไร)
- **GitHub Pages ค้าง** → เช็คว่า `.nojekyll` ยังอยู่
- **`index.html` ใหญ่มากแล้ว** — ของใหม่แยกไฟล์เสมอ

## คำสั่ง build Widget (กันลืม)

```bash
cd "/Users/agapae/Documents/Work PAE/Claude/Widget"
npm run build                       # ใช้ได้ปกติแล้ว
npm run tauri build                 # ได้ .app ที่ src-tauri/target/release/bundle/macos/

# ติดตั้งทับ (สำรองตัวเก่าก่อนเสมอ)
ditto "/Applications/AGAPAE Widget.app" "/Applications/AGAPAE Widget.app.bak-$(date +%Y%m%d-%H%M)"
rm -rf "/Applications/AGAPAE Widget.app"
ditto "src-tauri/target/release/bundle/macos/AGAPAE Widget.app" "/Applications/AGAPAE Widget.app"
```

> node เคยพังจาก Homebrew อัป `llhttp` เป็น 9.4.3 แต่ node 25.8.2 ลิงก์กับ 9.3 ไว้
> แก้แล้วด้วย `brew reinstall node` → ได้ **node 26.7.0 + npm 11.19.0** (ข้ามเมเจอร์ ไม่ใช่แค่ลงซ้ำ)
> ถ้าเจอโปรเจกต์ไหนพังเพราะ node 26 กดกลับได้ด้วย `brew install node@25`
